[
  {
    "pregunta": "dbt project review — a Databricks SQL warehouse deployment. SQL error appears before warehouse execution. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Only check warehouse query history, compile errors never reach warehouse.",
        "es_correcta": false,
        "explicacion": "Compile errors never reach warehouse."
      },
      {
        "texto_opcion": "Fix Jinja/SQL at compile time using `dbt compile` output.",
        "es_correcta": true,
        "explicacion": "Compile resolves refs/macros; run executes against warehouse."
      },
      {
        "texto_opcion": "Disable partial parsing forever as only fix.",
        "es_correcta": false,
        "explicacion": "Does not fix bad SQL."
      },
      {
        "texto_opcion": "Edit target/compiled files directly, overwritten on next compile.",
        "es_correcta": false,
        "explicacion": "Overwritten on next compile."
      }
    ],
    "tags": [
      "3.1",
      "compile vs run"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a finance mart with strict SLAs: verify how ref() resolved for a model. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Read manifest only for SQL text, manifest has parsed metadata.",
        "es_correcta": false,
        "explicacion": "manifest has parsed metadata; compiled has SQL."
      },
      {
        "texto_opcion": "Trust source SQL without compile",
        "es_correcta": false,
        "explicacion": "Jinja unresolved."
      },
      {
        "texto_opcion": "Inspect `target/compiled/<project>/models/...sql` after compile.",
        "es_correcta": true,
        "explicacion": "Compiled SQL shows fully rendered SQL sent to warehouse."
      },
      {
        "texto_opcion": "Change compiled SQL in production as the primary design.",
        "es_correcta": false,
        "explicacion": "Not source of truth."
      }
    ],
    "tags": [
      "3.1",
      "compiled sql"
    ]
  },
  {
    "pregunta": "In a BigQuery analytics repo: schema.yml tests not applied; YAML parser error. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Move tests to comments.",
        "es_correcta": false,
        "explicacion": "Not parsed."
      },
      {
        "texto_opcion": "Duplicate key names at same level, parser errors or overrides.",
        "es_correcta": false,
        "explicacion": "Parser errors or overrides."
      },
      {
        "texto_opcion": "Use tabs inconsistently in YAML, yAML forbids tab indentation.",
        "es_correcta": false,
        "explicacion": "YAML forbids tab indentation."
      },
      {
        "texto_opcion": "Fix YAML indentation and structure under models/columns.",
        "es_correcta": true,
        "explicacion": "Invalid YAML prevents schema tests and docs from loading."
      }
    ],
    "tags": [
      "3.1",
      "yaml indentation"
    ]
  },
  {
    "pregunta": "For a Snowflake warehouse project with nightly jobs: dbt finds two models with same name. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Disable one model without fixing name clash in parser.",
        "es_correcta": false,
        "explicacion": "May still conflict at parse."
      },
      {
        "texto_opcion": "Ensure unique model names across model-paths and packages.",
        "es_correcta": true,
        "explicacion": "Duplicate resource names cause compilation conflicts."
      },
      {
        "texto_opcion": "Ignore package collision, must resolve namespace or rename.",
        "es_correcta": false,
        "explicacion": "Must resolve namespace or rename."
      },
      {
        "texto_opcion": "Rename only file extension, model name comes from filename stem.",
        "es_correcta": false,
        "explicacion": "Model name comes from filename stem."
      }
    ],
    "tags": [
      "3.1",
      "duplicate model name"
    ]
  },
  {
    "pregunta": "In a Snowflake warehouse project: compilation error: model 'stg_order' not found. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Fix ref() name to match existing model `stg_orders`.",
        "es_correcta": true,
        "explicacion": "ref typos fail compilation with node not found."
      },
      {
        "texto_opcion": "Hardcode table instead of fixing ref as the default.",
        "es_correcta": false,
        "explicacion": "Technical debt."
      },
      {
        "texto_opcion": "Disable partial parsing as only action configured.",
        "es_correcta": false,
        "explicacion": "Typo remains."
      },
      {
        "texto_opcion": "Create empty placeholder model indefinitely as the default.",
        "es_correcta": false,
        "explicacion": "Masks logic gap."
      }
    ],
    "tags": [
      "3.1",
      "ref typo"
    ]
  },
  {
    "pregunta": "source('raw','orders') fails compilation. Context: a medallion lakehouse built with dbt.",
    "opciones": [
      {
        "texto_opcion": "Add matching source definition in sources YAML with same.",
        "es_correcta": true,
        "explicacion": "source() requires declared source and table identifiers."
      },
      {
        "texto_opcion": "Typo database only in SQL, must match YAML source config.",
        "es_correcta": false,
        "explicacion": "Must match YAML source config."
      },
      {
        "texto_opcion": "Use ref for raw table, wrong function for external data.",
        "es_correcta": false,
        "explicacion": "Wrong function for external data."
      },
      {
        "texto_opcion": "Remove sources YAML used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Breaks all source()."
      }
    ],
    "tags": [
      "3.1",
      "source not found"
    ]
  },
  {
    "pregunta": "For a finance mart with strict SLAs with nightly jobs: joined models produce ambiguous column in warehouse error. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Disable joins, does not solve transform need, does not solve transform need.",
        "es_correcta": false,
        "explicacion": "Does not solve transform need."
      },
      {
        "texto_opcion": "SELECT * on both sides of join, often causes ambiguity and schema drift.",
        "es_correcta": false,
        "explicacion": "Often causes ambiguity and schema drift."
      },
      {
        "texto_opcion": "Rename warehouse database, irrelevant to column names, irrelevant to column names.",
        "es_correcta": false,
        "explicacion": "Irrelevant to column names."
      },
      {
        "texto_opcion": "Qualify columns with alias in SELECT and join conditions.",
        "es_correcta": true,
        "explicacion": "Ambiguous columns occur when names collide without aliases."
      }
    ],
    "tags": [
      "3.1",
      "ambiguous column"
    ]
  },
  {
    "pregunta": "compilation error: macro 'pivot_columns' not found. Context: a platform team publishing shared packages.",
    "opciones": [
      {
        "texto_opcion": "Install package or define macro; run `dbt deps` if package macro.",
        "es_correcta": true,
        "explicacion": "Missing macros fail at compile unless defined locally."
      },
      {
        "texto_opcion": "Ignore and run --empty, still needs macro at compile for many commands.",
        "es_correcta": false,
        "explicacion": "Still needs macro at compile for many commands."
      },
      {
        "texto_opcion": "Delete packages directory in CI before deps, macros missing every run.",
        "es_correcta": false,
        "explicacion": "Macros missing every run."
      },
      {
        "texto_opcion": "Copy broken macro from blog without tests, may not match adapter.",
        "es_correcta": false,
        "explicacion": "May not match adapter."
      }
    ],
    "tags": [
      "3.1",
      "macro undefined"
    ]
  },
  {
    "pregunta": "In a platform team publishing shared packages: macro A calls B calls A causing stack issues. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Inline everything into one macro",
        "es_correcta": false,
        "explicacion": "Unmaintainable."
      },
      {
        "texto_opcion": "Refactor macros to remove circular dependencies.",
        "es_correcta": true,
        "explicacion": "Circular macro calls cause compile/runtime failures."
      },
      {
        "texto_opcion": "Disable logging applied across environments for consistency.",
        "es_correcta": false,
        "explicacion": "Hides issue."
      },
      {
        "texto_opcion": "Increase warehouse timeout only, does not fix recursion.",
        "es_correcta": false,
        "explicacion": "Does not fix recursion."
      }
    ],
    "tags": [
      "3.1",
      "circular macro"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: unexpected `{% endif %}` in model. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Remove all Jinja, loses parameterization; not the best fit here as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Loses parameterization."
      },
      {
        "texto_opcion": "Fix only run SQL not source, source must be valid Jinja, source must be valid Jinja.",
        "es_correcta": false,
        "explicacion": "Source must be valid Jinja."
      },
      {
        "texto_opcion": "Balance Jinja blocks ({% if %}/{% endif %}, {% macro %}/{% endmacro %}).",
        "es_correcta": true,
        "explicacion": "Mismatched Jinja tags cause parser errors."
      },
      {
        "texto_opcion": "Escape braces incorrectly in SQL as the default production choice here.",
        "es_correcta": false,
        "explicacion": "Breaks templating."
      }
    ],
    "tags": [
      "3.1",
      "parse error jinja"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. model runs but parent not built. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Build upstream with `dbt run --select +model` or full DAG order.",
        "es_correcta": true,
        "explicacion": "Runtime missing relation means dependency not materialized in target."
      },
      {
        "texto_opcion": "Create empty table manually with wrong schema, schema mismatch later.",
        "es_correcta": false,
        "explicacion": "Schema mismatch later."
      },
      {
        "texto_opcion": "Skip DAG order with random run applied across environments prioritized for simplicity, though it.",
        "es_correcta": false,
        "explicacion": "Dependency failures."
      },
      {
        "texto_opcion": "Use --empty expecting data, --empty skips data ops, --empty skips data ops.",
        "es_correcta": false,
        "explicacion": "--empty skips data ops; relation may exist without rows."
      }
    ],
    "tags": [
      "3.1",
      "relation does not exist runtime"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a CI pipeline using dbt Core: models land in unexpected schema dev_alice vs dev. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Check `target.schema`, custom schema macro, and `generate_schema_name`.",
        "es_correcta": true,
        "explicacion": "Custom schema logic affects final relation schema names."
      },
      {
        "texto_opcion": "Ignore profile target, wrong environment connection, wrong environment connection.",
        "es_correcta": false,
        "explicacion": "Wrong environment connection."
      },
      {
        "texto_opcion": "Delete profiles.yml adopted as the standard pattern for the team configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Cannot connect."
      },
      {
        "texto_opcion": "Hardcode prod schema in dev configured end-to-end for this workload.",
        "es_correcta": false,
        "explicacion": "Risky overwrites."
      }
    ],
    "tags": [
      "3.1",
      "target schema"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: need verbose logging for adapter SQL issued. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Run with `--debug` flag to increase log verbosity.",
        "es_correcta": true,
        "explicacion": "--debug prints detailed execution logs including adapter queries."
      },
      {
        "texto_opcion": "Share debug logs with secrets publicly.",
        "es_correcta": false,
        "explicacion": "Credential leak risk."
      },
      {
        "texto_opcion": "Disable all logging, blind troubleshooting; not the best fit here.",
        "es_correcta": false,
        "explicacion": "Blind troubleshooting."
      },
      {
        "texto_opcion": "Only screenshot UI without logs.",
        "es_correcta": false,
        "explicacion": "Insufficient detail."
      }
    ],
    "tags": [
      "3.1",
      "logs debug"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a platform team publishing shared packages: SQL error appears before warehouse execution. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Disable partial parsing forever as only fix.",
        "es_correcta": false,
        "explicacion": "Does not fix bad SQL."
      },
      {
        "texto_opcion": "Only check warehouse query history, compile errors never reach warehouse.",
        "es_correcta": false,
        "explicacion": "Compile errors never reach warehouse."
      },
      {
        "texto_opcion": "Fix Jinja/SQL at compile time using `dbt compile` output.",
        "es_correcta": true,
        "explicacion": "Compile resolves refs/macros; run executes against warehouse."
      },
      {
        "texto_opcion": "Edit target/compiled files directly, overwritten on next compile.",
        "es_correcta": false,
        "explicacion": "Overwritten on next compile."
      }
    ],
    "tags": [
      "3.1",
      "compile vs run"
    ]
  },
  {
    "pregunta": "dbt project review — a Snowflake warehouse project. verify how ref() resolved for a model. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Read manifest only for SQL text — manifest has parsed metadata; compiled has SQL.",
        "es_correcta": false,
        "explicacion": "manifest has parsed metadata; compiled has SQL."
      },
      {
        "texto_opcion": "Change compiled SQL in production as the default",
        "es_correcta": false,
        "explicacion": "Not source of truth."
      },
      {
        "texto_opcion": "Trust source SQL without compile",
        "es_correcta": false,
        "explicacion": "Jinja unresolved."
      },
      {
        "texto_opcion": "Inspect `target/compiled/<project>/models/...sql` after compile.",
        "es_correcta": true,
        "explicacion": "Compiled SQL shows fully rendered SQL sent to warehouse."
      }
    ],
    "tags": [
      "3.1",
      "compiled sql"
    ]
  },
  {
    "pregunta": "For a medallion lakehouse built with dbt with nightly jobs: schema.yml tests not applied; YAML parser error. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Fix YAML indentation and structure under models/columns.",
        "es_correcta": true,
        "explicacion": "Invalid YAML prevents schema tests and docs from loading."
      },
      {
        "texto_opcion": "Use tabs inconsistently in YAML, yAML forbids tab indentation.",
        "es_correcta": false,
        "explicacion": "YAML forbids tab indentation."
      },
      {
        "texto_opcion": "Duplicate key names at same level, parser errors or overrides.",
        "es_correcta": false,
        "explicacion": "Parser errors or overrides."
      },
      {
        "texto_opcion": "Move tests to comments used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Not parsed."
      }
    ],
    "tags": [
      "3.1",
      "yaml indentation"
    ]
  },
  {
    "pregunta": "For a Snowflake warehouse project with nightly jobs: dbt finds two models with same name. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Disable one model without fixing name clash in parser.",
        "es_correcta": false,
        "explicacion": "May still conflict at parse."
      },
      {
        "texto_opcion": "Ignore package collision, must resolve namespace or rename.",
        "es_correcta": false,
        "explicacion": "Must resolve namespace or rename."
      },
      {
        "texto_opcion": "Rename only file extension, model name comes from filename stem.",
        "es_correcta": false,
        "explicacion": "Model name comes from filename stem."
      },
      {
        "texto_opcion": "Ensure unique model names across model-paths and packages.",
        "es_correcta": true,
        "explicacion": "Duplicate resource names cause compilation conflicts."
      }
    ],
    "tags": [
      "3.1",
      "duplicate model name"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: compilation error: model 'stg_order' not found. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Disable partial parsing as only action.",
        "es_correcta": false,
        "explicacion": "Typo remains."
      },
      {
        "texto_opcion": "Hardcode table instead of fixing ref.",
        "es_correcta": false,
        "explicacion": "Technical debt."
      },
      {
        "texto_opcion": "Create empty placeholder model indefinitely.",
        "es_correcta": false,
        "explicacion": "Masks logic gap."
      },
      {
        "texto_opcion": "Fix ref() name to match existing model `stg_orders`.",
        "es_correcta": true,
        "explicacion": "ref typos fail compilation with node not found."
      }
    ],
    "tags": [
      "3.1",
      "ref typo"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: source('raw','orders') fails compilation. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use ref for raw table, wrong function for external data.",
        "es_correcta": false,
        "explicacion": "Wrong function for external data."
      },
      {
        "texto_opcion": "Remove sources YAML",
        "es_correcta": false,
        "explicacion": "Breaks all source()."
      },
      {
        "texto_opcion": "Add matching source definition in sources YAML with same.",
        "es_correcta": true,
        "explicacion": "source() requires declared source and table identifiers."
      },
      {
        "texto_opcion": "Typo database only in SQL, must match YAML source config.",
        "es_correcta": false,
        "explicacion": "Must match YAML source config."
      }
    ],
    "tags": [
      "3.1",
      "source not found"
    ]
  },
  {
    "pregunta": "In a CI pipeline using dbt Core: joined models produce ambiguous column in warehouse error. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "SELECT * on both sides of join, often causes ambiguity and schema drift.",
        "es_correcta": false,
        "explicacion": "Often causes ambiguity and schema drift."
      },
      {
        "texto_opcion": "Qualify columns with alias in SELECT and join conditions.",
        "es_correcta": true,
        "explicacion": "Ambiguous columns occur when names collide without aliases."
      },
      {
        "texto_opcion": "Disable joins, does not solve transform need, does not solve transform need.",
        "es_correcta": false,
        "explicacion": "Does not solve transform need."
      },
      {
        "texto_opcion": "Rename warehouse database, irrelevant to column names, irrelevant to column names.",
        "es_correcta": false,
        "explicacion": "Irrelevant to column names."
      }
    ],
    "tags": [
      "3.1",
      "ambiguous column"
    ]
  },
  {
    "pregunta": "For a monorepo with three dbt projects with nightly jobs: compilation error: macro 'pivot_columns' not found. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Delete packages directory in CI before deps, macros missing every run.",
        "es_correcta": false,
        "explicacion": "Macros missing every run."
      },
      {
        "texto_opcion": "Copy broken macro from blog without tests, may not match adapter.",
        "es_correcta": false,
        "explicacion": "May not match adapter."
      },
      {
        "texto_opcion": "Ignore and run --empty, still needs macro at compile for many commands.",
        "es_correcta": false,
        "explicacion": "Still needs macro at compile for many commands."
      },
      {
        "texto_opcion": "Install package or define macro; run `dbt deps` if package macro.",
        "es_correcta": true,
        "explicacion": "Missing macros fail at compile unless defined locally."
      }
    ],
    "tags": [
      "3.1",
      "macro undefined"
    ]
  },
  {
    "pregunta": "dbt project review — a CI pipeline using dbt Core. macro A calls B calls A causing stack issues. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Increase warehouse timeout only, does not fix recursion.",
        "es_correcta": false,
        "explicacion": "Does not fix recursion."
      },
      {
        "texto_opcion": "Refactor macros to remove circular dependencies.",
        "es_correcta": true,
        "explicacion": "Circular macro calls cause compile/runtime failures."
      },
      {
        "texto_opcion": "Disable logging",
        "es_correcta": false,
        "explicacion": "Hides issue."
      },
      {
        "texto_opcion": "Inline everything into one macro as the default production.",
        "es_correcta": false,
        "explicacion": "Unmaintainable."
      }
    ],
    "tags": [
      "3.1",
      "circular macro"
    ]
  },
  {
    "pregunta": "In a monorepo with three dbt projects: unexpected `{% endif %}` in model. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Fix only run SQL not source, source must be valid Jinja as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Source must be valid Jinja."
      },
      {
        "texto_opcion": "Balance Jinja blocks ({% if %}/{% endif %}, {% macro %}/{% endmacro %}).",
        "es_correcta": true,
        "explicacion": "Mismatched Jinja tags cause parser errors."
      },
      {
        "texto_opcion": "Escape braces incorrectly in SQL configured end-to-end for as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Breaks templating."
      },
      {
        "texto_opcion": "Remove all Jinja, loses parameterization; not the best fit here",
        "es_correcta": false,
        "explicacion": "Loses parameterization."
      }
    ],
    "tags": [
      "3.1",
      "parse error jinja"
    ]
  },
  {
    "pregunta": "In a platform team publishing shared packages: model runs but parent not built. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Build upstream with `dbt run --select +model` or full DAG order.",
        "es_correcta": true,
        "explicacion": "Runtime missing relation means dependency not materialized in target."
      },
      {
        "texto_opcion": "Create empty table manually with wrong schema, schema mismatch later.",
        "es_correcta": false,
        "explicacion": "Schema mismatch later."
      },
      {
        "texto_opcion": "Use --empty expecting data, --empty skips data ops, --empty skips data ops.",
        "es_correcta": false,
        "explicacion": "--empty skips data ops; relation may exist without rows."
      },
      {
        "texto_opcion": "Skip DAG order with random run used as the main path in",
        "es_correcta": false,
        "explicacion": "Dependency failures."
      }
    ],
    "tags": [
      "3.1",
      "relation does not exist runtime"
    ]
  },
  {
    "pregunta": "For a multi-environment dbt Cloud job with nightly jobs: models land in unexpected schema dev_alice vs dev. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Ignore profile target, wrong environment connection used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Wrong environment connection."
      },
      {
        "texto_opcion": "Check `target.schema`, custom schema macro, and `generate_schema_name`.",
        "es_correcta": true,
        "explicacion": "Custom schema logic affects final relation schema names."
      },
      {
        "texto_opcion": "Delete profiles.yml used as the main path in this architecture prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "Cannot connect."
      },
      {
        "texto_opcion": "Hardcode prod schema in dev prioritized for operational often seen in legacy designs but.",
        "es_correcta": false,
        "explicacion": "Risky overwrites."
      }
    ],
    "tags": [
      "3.1",
      "target schema"
    ]
  },
  {
    "pregunta": "dbt project review — a finance mart with strict SLAs. need verbose logging for adapter SQL issued. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Run with `--debug` flag to increase log verbosity.",
        "es_correcta": true,
        "explicacion": "--debug prints detailed execution logs including adapter queries."
      },
      {
        "texto_opcion": "Only screenshot UI without logs applied across",
        "es_correcta": false,
        "explicacion": "Insufficient detail."
      },
      {
        "texto_opcion": "Share debug logs with secrets publicly adopted as the often seen in legacy designs but not.",
        "es_correcta": false,
        "explicacion": "Credential leak risk."
      },
      {
        "texto_opcion": "Disable all logging, blind troubleshooting; not the best fit here.",
        "es_correcta": false,
        "explicacion": "Blind troubleshooting."
      }
    ],
    "tags": [
      "3.1",
      "logs debug"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a multi-environment dbt Cloud job: SQL error appears before warehouse execution. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Edit target/compiled files directly, overwritten on next compile.",
        "es_correcta": false,
        "explicacion": "Overwritten on next compile."
      },
      {
        "texto_opcion": "Fix Jinja/SQL at compile time using `dbt compile` output.",
        "es_correcta": true,
        "explicacion": "Compile resolves refs/macros; run executes against warehouse."
      },
      {
        "texto_opcion": "Only check warehouse query history, compile errors never reach warehouse.",
        "es_correcta": false,
        "explicacion": "Compile errors never reach warehouse."
      },
      {
        "texto_opcion": "Disable partial parsing forever as only fix used as the configured end-to-end.",
        "es_correcta": false,
        "explicacion": "Does not fix bad SQL."
      }
    ],
    "tags": [
      "3.1",
      "compile vs run"
    ]
  },
  {
    "pregunta": "dbt project review — a platform team publishing shared packages. verify how ref() resolved for a model. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Read manifest only for SQL text — manifest has parsed metadata; compiled has SQL.",
        "es_correcta": false,
        "explicacion": "manifest has parsed metadata; compiled has SQL."
      },
      {
        "texto_opcion": "Inspect `target/compiled/<project>/models/...sql` after compile.",
        "es_correcta": true,
        "explicacion": "Compiled SQL shows fully rendered SQL sent to warehouse."
      },
      {
        "texto_opcion": "Change compiled SQL in production selected to meet the as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Not source of truth."
      },
      {
        "texto_opcion": "Trust source SQL without compile as the primary design for",
        "es_correcta": false,
        "explicacion": "Jinja unresolved."
      }
    ],
    "tags": [
      "3.1",
      "compiled sql"
    ]
  },
  {
    "pregunta": "dbt project review — a Snowflake warehouse project. schema.yml tests not applied; YAML parser error. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Fix YAML indentation and structure under models/columns.",
        "es_correcta": true,
        "explicacion": "Invalid YAML prevents schema tests and docs from loading."
      },
      {
        "texto_opcion": "Move tests to comments applied across environments for consistency.",
        "es_correcta": false,
        "explicacion": "Not parsed."
      },
      {
        "texto_opcion": "Use tabs inconsistently in YAML, yAML forbids tab indentation.",
        "es_correcta": false,
        "explicacion": "YAML forbids tab indentation."
      },
      {
        "texto_opcion": "Duplicate key names at same level, parser errors or overrides.",
        "es_correcta": false,
        "explicacion": "Parser errors or overrides."
      }
    ],
    "tags": [
      "3.1",
      "yaml indentation"
    ]
  },
  {
    "pregunta": "In a monorepo with three dbt projects: dbt finds two models with same name. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Rename only file extension, model name comes from filename stem.",
        "es_correcta": false,
        "explicacion": "Model name comes from filename stem."
      },
      {
        "texto_opcion": "Disable one model without fixing name clash in parser.",
        "es_correcta": false,
        "explicacion": "May still conflict at parse."
      },
      {
        "texto_opcion": "Ignore package collision, must resolve namespace or rename.",
        "es_correcta": false,
        "explicacion": "Must resolve namespace or rename."
      },
      {
        "texto_opcion": "Ensure unique model names across model-paths and packages.",
        "es_correcta": true,
        "explicacion": "Duplicate resource names cause compilation conflicts."
      }
    ],
    "tags": [
      "3.1",
      "duplicate model name"
    ]
  },
  {
    "pregunta": "compilation error: model 'stg_order' not found. Context: a BigQuery analytics repo. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Disable partial parsing as only action.",
        "es_correcta": false,
        "explicacion": "Typo remains."
      },
      {
        "texto_opcion": "Fix ref() name to match existing model `stg_orders`.",
        "es_correcta": true,
        "explicacion": "ref typos fail compilation with node not found."
      },
      {
        "texto_opcion": "Hardcode table instead of fixing ref.",
        "es_correcta": false,
        "explicacion": "Technical debt."
      },
      {
        "texto_opcion": "Create empty placeholder model indefinitely applied across.",
        "es_correcta": false,
        "explicacion": "Masks logic gap."
      }
    ],
    "tags": [
      "3.1",
      "ref typo"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Databricks SQL warehouse deployment: source('raw','orders') fails compilation. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Use ref for raw table, wrong function for external data.",
        "es_correcta": false,
        "explicacion": "Wrong function for external data."
      },
      {
        "texto_opcion": "Remove sources YAML as the default production choice here.",
        "es_correcta": false,
        "explicacion": "Breaks all source()."
      },
      {
        "texto_opcion": "Add matching source definition in sources YAML with same.",
        "es_correcta": true,
        "explicacion": "source() requires declared source and table identifiers."
      },
      {
        "texto_opcion": "Typo database only in SQL, must match YAML source config.",
        "es_correcta": false,
        "explicacion": "Must match YAML source config."
      }
    ],
    "tags": [
      "3.1",
      "source not found"
    ]
  },
  {
    "pregunta": "joined models produce ambiguous column in warehouse error. Context: a Databricks SQL warehouse deployment. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Qualify columns with alias in SELECT and join conditions.",
        "es_correcta": true,
        "explicacion": "Ambiguous columns occur when names collide without aliases."
      },
      {
        "texto_opcion": "SELECT * on both sides of join, often causes ambiguity and schema drift.",
        "es_correcta": false,
        "explicacion": "Often causes ambiguity and schema drift."
      },
      {
        "texto_opcion": "Disable joins, does not solve transform need, does not solve transform need.",
        "es_correcta": false,
        "explicacion": "Does not solve transform need."
      },
      {
        "texto_opcion": "Rename warehouse database, irrelevant to column names, irrelevant to column names.",
        "es_correcta": false,
        "explicacion": "Irrelevant to column names."
      }
    ],
    "tags": [
      "3.1",
      "ambiguous column"
    ]
  },
  {
    "pregunta": "dbt project review — a Databricks SQL warehouse deployment. compilation error: macro 'pivot_columns' not found. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Copy broken macro from blog without tests, may not match adapter.",
        "es_correcta": false,
        "explicacion": "May not match adapter."
      },
      {
        "texto_opcion": "Install package or define macro; run `dbt deps` if package macro.",
        "es_correcta": true,
        "explicacion": "Missing macros fail at compile unless defined locally."
      },
      {
        "texto_opcion": "Delete packages directory in CI before deps, macros missing every run.",
        "es_correcta": false,
        "explicacion": "Macros missing every run."
      },
      {
        "texto_opcion": "Ignore and run --empty, still needs macro at compile for many commands.",
        "es_correcta": false,
        "explicacion": "Still needs macro at compile for many commands."
      }
    ],
    "tags": [
      "3.1",
      "macro undefined"
    ]
  },
  {
    "pregunta": "In a medallion lakehouse built with dbt: macro A calls B calls A causing stack issues. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Increase warehouse timeout only, does not fix recursion.",
        "es_correcta": false,
        "explicacion": "Does not fix recursion."
      },
      {
        "texto_opcion": "Disable logging used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Hides issue."
      },
      {
        "texto_opcion": "Inline everything into one macro.",
        "es_correcta": false,
        "explicacion": "Unmaintainable."
      },
      {
        "texto_opcion": "Refactor macros to remove circular dependencies.",
        "es_correcta": true,
        "explicacion": "Circular macro calls cause compile/runtime failures."
      }
    ],
    "tags": [
      "3.1",
      "circular macro"
    ]
  },
  {
    "pregunta": "unexpected `{% endif %}` in model. Context: a finance mart with strict SLAs. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Remove all Jinja, loses parameterization; not the best fit here applied across environments for operational consistency.",
        "es_correcta": false,
        "explicacion": "Loses parameterization."
      },
      {
        "texto_opcion": "Escape braces incorrectly in SQL selected to meet the stated configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Breaks templating."
      },
      {
        "texto_opcion": "Balance Jinja blocks ({% if %}/{% endif %}, {% macro %}/{% endmacro %}).",
        "es_correcta": true,
        "explicacion": "Mismatched Jinja tags cause parser errors."
      },
      {
        "texto_opcion": "Fix only run SQL not source, source must be valid Jinja applied across environments for as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Source must be valid Jinja."
      }
    ],
    "tags": [
      "3.1",
      "parse error jinja"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a finance mart with strict SLAs: model runs but parent not built. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Create empty table manually with wrong schema, schema mismatch later.",
        "es_correcta": false,
        "explicacion": "Schema mismatch later."
      },
      {
        "texto_opcion": "Build upstream with `dbt run --select +model` or full DAG order.",
        "es_correcta": true,
        "explicacion": "Runtime missing relation means dependency not materialized in target."
      },
      {
        "texto_opcion": "Use --empty expecting data, --empty skips data ops, --empty skips data ops.",
        "es_correcta": false,
        "explicacion": "--empty skips data ops; relation may exist without rows."
      },
      {
        "texto_opcion": "Skip DAG order with random run prioritized for operational as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Dependency failures."
      }
    ],
    "tags": [
      "3.1",
      "relation does not exist runtime"
    ]
  },
  {
    "pregunta": "models land in unexpected schema dev_alice vs dev. Context: a finance mart with strict SLAs. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Check `target.schema`, custom schema macro, and `generate_schema_name`.",
        "es_correcta": true,
        "explicacion": "Custom schema logic affects final relation schema names."
      },
      {
        "texto_opcion": "Hardcode prod schema in dev adopted as the standard pattern plausible at first glance, yet.",
        "es_correcta": false,
        "explicacion": "Risky overwrites."
      },
      {
        "texto_opcion": "Delete profiles.yml applied across environments for consistency applied across environments for operational consistency.",
        "es_correcta": false,
        "explicacion": "Cannot connect."
      },
      {
        "texto_opcion": "Ignore profile target, wrong environment connection often seen in legacy designs but not.",
        "es_correcta": false,
        "explicacion": "Wrong environment connection."
      }
    ],
    "tags": [
      "3.1",
      "target schema"
    ]
  },
  {
    "pregunta": "dbt project review — a finance mart with strict SLAs. need verbose logging for adapter SQL issued. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Only screenshot UI without logs.",
        "es_correcta": false,
        "explicacion": "Insufficient detail."
      },
      {
        "texto_opcion": "Share debug logs with secrets publicly.",
        "es_correcta": false,
        "explicacion": "Credential leak risk."
      },
      {
        "texto_opcion": "Run with `--debug` flag to increase log verbosity.",
        "es_correcta": true,
        "explicacion": "--debug prints detailed execution logs including adapter queries."
      },
      {
        "texto_opcion": "Disable all logging, blind troubleshooting; not the best fit here.",
        "es_correcta": false,
        "explicacion": "Blind troubleshooting."
      }
    ],
    "tags": [
      "3.1",
      "logs debug"
    ]
  },
  {
    "pregunta": "dbt project review — a BigQuery analytics repo. SQL error appears before warehouse execution. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Edit target/compiled files directly, overwritten on next compile.",
        "es_correcta": false,
        "explicacion": "Overwritten on next compile."
      },
      {
        "texto_opcion": "Disable partial parsing forever as only fix",
        "es_correcta": false,
        "explicacion": "Does not fix bad SQL."
      },
      {
        "texto_opcion": "Fix Jinja/SQL at compile time using `dbt compile` output.",
        "es_correcta": true,
        "explicacion": "Compile resolves refs/macros; run executes against warehouse."
      },
      {
        "texto_opcion": "Only check warehouse query history, compile errors never reach warehouse.",
        "es_correcta": false,
        "explicacion": "Compile errors never reach warehouse."
      }
    ],
    "tags": [
      "3.1",
      "compile vs run"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. verify how ref() resolved for a model. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Inspect `target/compiled/<project>/models/...sql` after compile.",
        "es_correcta": true,
        "explicacion": "Compiled SQL shows fully rendered SQL sent to warehouse."
      },
      {
        "texto_opcion": "Change compiled SQL in production",
        "es_correcta": false,
        "explicacion": "Not source of truth."
      },
      {
        "texto_opcion": "Trust source SQL without compile.",
        "es_correcta": false,
        "explicacion": "Jinja unresolved."
      },
      {
        "texto_opcion": "Read manifest only for SQL text, manifest has parsed metadata.",
        "es_correcta": false,
        "explicacion": "manifest has parsed metadata; compiled has SQL."
      }
    ],
    "tags": [
      "3.1",
      "compiled sql"
    ]
  },
  {
    "pregunta": "For a medallion lakehouse built with dbt with nightly jobs: schema.yml tests not applied; YAML parser error. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Fix YAML indentation and structure under models/columns.",
        "es_correcta": true,
        "explicacion": "Invalid YAML prevents schema tests and docs from loading."
      },
      {
        "texto_opcion": "Move tests to comments as the primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Not parsed."
      },
      {
        "texto_opcion": "Duplicate key names at same level, parser errors or overrides.",
        "es_correcta": false,
        "explicacion": "Parser errors or overrides."
      },
      {
        "texto_opcion": "Use tabs inconsistently in YAML, yAML forbids tab indentation.",
        "es_correcta": false,
        "explicacion": "YAML forbids tab indentation."
      }
    ],
    "tags": [
      "3.1",
      "yaml indentation"
    ]
  },
  {
    "pregunta": "In a Databricks SQL warehouse deployment: dbt finds two models with same name. Which dbt approach is best? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Ensure unique model names across model-paths and packages.",
        "es_correcta": true,
        "explicacion": "Duplicate resource names cause compilation conflicts."
      },
      {
        "texto_opcion": "Disable one model without fixing name clash in parser.",
        "es_correcta": false,
        "explicacion": "May still conflict at parse."
      },
      {
        "texto_opcion": "Ignore package collision, must resolve namespace or rename.",
        "es_correcta": false,
        "explicacion": "Must resolve namespace or rename."
      },
      {
        "texto_opcion": "Rename only file extension, model name comes from filename stem.",
        "es_correcta": false,
        "explicacion": "Model name comes from filename stem."
      }
    ],
    "tags": [
      "3.1",
      "duplicate model name"
    ]
  },
  {
    "pregunta": "dbt project review — a Snowflake warehouse project. compilation error: model 'stg_order' not found. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Hardcode table instead of fixing ref.",
        "es_correcta": false,
        "explicacion": "Technical debt."
      },
      {
        "texto_opcion": "Create empty placeholder model indefinitely prioritized.",
        "es_correcta": false,
        "explicacion": "Masks logic gap."
      },
      {
        "texto_opcion": "Disable partial parsing as only action.",
        "es_correcta": false,
        "explicacion": "Typo remains."
      },
      {
        "texto_opcion": "Fix ref() name to match existing model `stg_orders`.",
        "es_correcta": true,
        "explicacion": "ref typos fail compilation with node not found."
      }
    ],
    "tags": [
      "3.1",
      "ref typo"
    ]
  },
  {
    "pregunta": "dbt project review — a medallion lakehouse built with dbt. source('raw','orders') fails compilation. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Typo database only in SQL, must match YAML source config.",
        "es_correcta": false,
        "explicacion": "Must match YAML source config."
      },
      {
        "texto_opcion": "Add matching source definition in sources YAML with same.",
        "es_correcta": true,
        "explicacion": "source() requires declared source and table identifiers."
      },
      {
        "texto_opcion": "Remove sources YAML used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Breaks all source()."
      },
      {
        "texto_opcion": "Use ref for raw table, wrong function for external data.",
        "es_correcta": false,
        "explicacion": "Wrong function for external data."
      }
    ],
    "tags": [
      "3.1",
      "source not found"
    ]
  },
  {
    "pregunta": "For a Snowflake warehouse project with nightly jobs: joined models produce ambiguous column in warehouse error. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Rename warehouse database, irrelevant to column names, irrelevant to column names.",
        "es_correcta": false,
        "explicacion": "Irrelevant to column names."
      },
      {
        "texto_opcion": "SELECT * on both sides of join, often causes ambiguity and schema drift.",
        "es_correcta": false,
        "explicacion": "Often causes ambiguity and schema drift."
      },
      {
        "texto_opcion": "Qualify columns with alias in SELECT and join conditions.",
        "es_correcta": true,
        "explicacion": "Ambiguous columns occur when names collide without aliases."
      },
      {
        "texto_opcion": "Disable joins, does not solve transform need, does not solve transform need.",
        "es_correcta": false,
        "explicacion": "Does not solve transform need."
      }
    ],
    "tags": [
      "3.1",
      "ambiguous column"
    ]
  },
  {
    "pregunta": "In a product analytics team using dbt: compilation error: macro 'pivot_columns' not found. Which dbt approach is best? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Install package or define macro; run `dbt deps` if package macro.",
        "es_correcta": true,
        "explicacion": "Missing macros fail at compile unless defined locally."
      },
      {
        "texto_opcion": "Copy broken macro from blog without tests, may not match adapter.",
        "es_correcta": false,
        "explicacion": "May not match adapter."
      },
      {
        "texto_opcion": "Delete packages directory in CI before deps, macros missing every run.",
        "es_correcta": false,
        "explicacion": "Macros missing every run."
      },
      {
        "texto_opcion": "Ignore and run --empty, still needs macro at compile for many commands.",
        "es_correcta": false,
        "explicacion": "Still needs macro at compile for many commands."
      }
    ],
    "tags": [
      "3.1",
      "macro undefined"
    ]
  },
  {
    "pregunta": "macro A calls B calls A causing stack issues. Context: a CI pipeline using dbt Core. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Increase warehouse timeout only, does not fix recursion.",
        "es_correcta": false,
        "explicacion": "Does not fix recursion."
      },
      {
        "texto_opcion": "Disable logging as the primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Hides issue."
      },
      {
        "texto_opcion": "Inline everything into one macro applied across.",
        "es_correcta": false,
        "explicacion": "Unmaintainable."
      },
      {
        "texto_opcion": "Refactor macros to remove circular dependencies.",
        "es_correcta": true,
        "explicacion": "Circular macro calls cause compile/runtime failures."
      }
    ],
    "tags": [
      "3.1",
      "circular macro"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a finance mart with strict SLAs: unexpected `{% endif %}` in model. Select the correct answer. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Fix only run SQL not source, source must be valid Jinja",
        "es_correcta": false,
        "explicacion": "Source must be valid Jinja."
      },
      {
        "texto_opcion": "Escape braces incorrectly in SQL",
        "es_correcta": false,
        "explicacion": "Breaks templating."
      },
      {
        "texto_opcion": "Balance Jinja blocks ({% if %}/{% endif %}, {% macro %}/{% endmacro %}).",
        "es_correcta": true,
        "explicacion": "Mismatched Jinja tags cause parser errors."
      },
      {
        "texto_opcion": "Remove all Jinja, loses parameterization; not the best fit here often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "Loses parameterization."
      }
    ],
    "tags": [
      "3.1",
      "parse error jinja"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: model runs but parent not built. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Create empty table manually with wrong schema, schema mismatch later.",
        "es_correcta": false,
        "explicacion": "Schema mismatch later."
      },
      {
        "texto_opcion": "Use --empty expecting data, --empty skips data ops, --empty skips data ops.",
        "es_correcta": false,
        "explicacion": "--empty skips data ops; relation may exist without rows."
      },
      {
        "texto_opcion": "Build upstream with `dbt run --select +model` or full DAG order.",
        "es_correcta": true,
        "explicacion": "Runtime missing relation means dependency not materialized in target."
      },
      {
        "texto_opcion": "Skip DAG order with random run",
        "es_correcta": false,
        "explicacion": "Dependency failures."
      }
    ],
    "tags": [
      "3.1",
      "relation does not exist runtime"
    ]
  },
  {
    "pregunta": "For a medallion lakehouse built with dbt with nightly jobs: models land in unexpected schema dev_alice vs dev. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Delete profiles.yml",
        "es_correcta": false,
        "explicacion": "Cannot connect."
      },
      {
        "texto_opcion": "Hardcode prod schema in dev used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Risky overwrites."
      },
      {
        "texto_opcion": "Check `target.schema`, custom schema macro, and `generate_schema_name`.",
        "es_correcta": true,
        "explicacion": "Custom schema logic affects final relation schema names."
      },
      {
        "texto_opcion": "Ignore profile target, wrong environment connection, wrong environment connection.",
        "es_correcta": false,
        "explicacion": "Wrong environment connection."
      }
    ],
    "tags": [
      "3.1",
      "target schema"
    ]
  }
]
