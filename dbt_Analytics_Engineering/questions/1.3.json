[
  {
    "pregunta": "dbt project review — a finance mart with strict SLAs. model SQL must read another dbt model `stg_orders`. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Use source() for another dbt model",
        "es_correcta": false,
        "explicacion": "source() is for raw sources, not dbt models."
      },
      {
        "texto_opcion": "Hardcode `analytics.stg_orders`",
        "es_correcta": false,
        "explicacion": "Bypasses environment routing and lineage."
      },
      {
        "texto_opcion": "Use `SELECT ... FROM {{ ref('stg_orders') }}`",
        "es_correcta": true,
        "explicacion": "ref() resolves dependencies and builds the DAG with environment-aware relations."
      },
      {
        "texto_opcion": "Copy stg_orders SQL inline",
        "es_correcta": false,
        "explicacion": "Breaks modularity and lineage."
      }
    ],
    "tags": [
      "1.3",
      "ref function"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: model reads raw landing table loaded by ELT outside dbt. Best practice?",
    "opciones": [
      {
        "texto_opcion": "ref('raw_orders') without a model",
        "es_correcta": false,
        "explicacion": "ref expects a dbt model, not external raw table."
      },
      {
        "texto_opcion": "Hardcode database.schema.table only",
        "es_correcta": false,
        "explicacion": "Loses source freshness and lineage in docs."
      },
      {
        "texto_opcion": "Create empty model with no SQL",
        "es_correcta": false,
        "explicacion": "Does not define the external table as a source."
      },
      {
        "texto_opcion": "Use `{{ source('raw', 'orders') }}` declared in sources YAML",
        "es_correcta": true,
        "explicacion": "source() references declared sources and enables freshness tests."
      }
    ],
    "tags": [
      "1.3",
      "source function"
    ]
  },
  {
    "pregunta": "only downstream of `stg_customers` changed and must rebuild. Context: a multi-environment dbt Cloud job.",
    "opciones": [
      {
        "texto_opcion": "--select +stg_customers only upstream",
        "es_correcta": false,
        "explicacion": "Wrong direction; need downstream + suffix."
      },
      {
        "texto_opcion": "Run `dbt run --select stg_customers+`",
        "es_correcta": true,
        "explicacion": "+ selects downstream dependents in the DAG."
      },
      {
        "texto_opcion": "dbt run without select every time",
        "es_correcta": false,
        "explicacion": "Rebuilds entire project unnecessarily."
      },
      {
        "texto_opcion": "Manually run warehouse SQL per table",
        "es_correcta": false,
        "explicacion": "Outside dbt orchestration."
      }
    ],
    "tags": [
      "1.3",
      "dag selection"
    ]
  },
  {
    "pregunta": "fix requires rebuilding a mart and all its parents. Context: a BigQuery analytics repo.",
    "opciones": [
      {
        "texto_opcion": "Run `dbt run --select +fct_orders`",
        "es_correcta": true,
        "explicacion": "+ prefix selects upstream ancestors of the model."
      },
      {
        "texto_opcion": "Drop child models first manually",
        "es_correcta": false,
        "explicacion": "Risky; selector handles order."
      },
      {
        "texto_opcion": "dbt test only",
        "es_correcta": false,
        "explicacion": "Does not rebuild models."
      },
      {
        "texto_opcion": "--select fct_orders+ for parents",
        "es_correcta": false,
        "explicacion": "Suffix + is downstream; prefix + is upstream."
      }
    ],
    "tags": [
      "1.3",
      "upstream select"
    ]
  },
  {
    "pregunta": "dbt project review — a finance mart with strict SLAs. run staging folder and exclude deprecated models tagged legacy. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Exclude by renaming files randomly",
        "es_correcta": false,
        "explicacion": "Not declarative."
      },
      {
        "texto_opcion": "Run entire project always",
        "es_correcta": false,
        "explicacion": "Wastes compute."
      },
      {
        "texto_opcion": "Use only model name with no path/tag",
        "es_correcta": false,
        "explicacion": "Harder to maintain at scale."
      },
      {
        "texto_opcion": "Use `dbt run --select path:models/staging --exclude tag:legacy`",
        "es_correcta": true,
        "explicacion": "Graph operators combine path, tag, and exclude filters."
      }
    ],
    "tags": [
      "1.3",
      "graph operators"
    ]
  },
  {
    "pregunta": "dbt project review — a medallion lakehouse built with dbt. intermediate logic used once should not materialize. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Use seeds for intermediates",
        "es_correcta": false,
        "explicacion": "Wrong tool for transforms."
      },
      {
        "texto_opcion": "Materialize every step as table",
        "es_correcta": false,
        "explicacion": "Table sprawl and cost."
      },
      {
        "texto_opcion": "Duplicate SQL in five models",
        "es_correcta": false,
        "explicacion": "Maintenance burden."
      },
      {
        "texto_opcion": "Use ephemeral model or a SQL CTE via ref to ephemeral",
        "es_correcta": true,
        "explicacion": "Ephemeral/CTE avoids extra tables in the warehouse."
      }
    ],
    "tags": [
      "1.3",
      "cte vs ephemeral"
    ]
  },
  {
    "pregunta": "In a Snowflake warehouse project: dbt compile fails citing a cyclic dependency. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Remove circular ref() chains so the DAG is acyclic",
        "es_correcta": true,
        "explicacion": "dbt requires a directed acyclic graph; cycles break compilation."
      },
      {
        "texto_opcion": "Add cycles for 'performance'",
        "es_correcta": false,
        "explicacion": "Invalid dbt design."
      },
      {
        "texto_opcion": "Replace ref() with hardcoded tables",
        "es_correcta": false,
        "explicacion": "Breaks lineage permanently."
      },
      {
        "texto_opcion": "Ignore the error and run --full-refresh",
        "es_correcta": false,
        "explicacion": "Cycle remains; run fails."
      }
    ],
    "tags": [
      "1.3",
      "cycle detection"
    ]
  },
  {
    "pregunta": "In a finance mart with strict SLAs: raw sources should be lightly cleaned before marts. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "One monolithic 5000-line model",
        "es_correcta": false,
        "explicacion": "Hard to test and maintain."
      },
      {
        "texto_opcion": "Mart models source() directly with heavy logic",
        "es_correcta": false,
        "explicacion": "Couples marts to raw layout."
      },
      {
        "texto_opcion": "Build staging models with source() then ref staging from marts",
        "es_correcta": true,
        "explicacion": "Staging isolates source changes and standardizes naming."
      },
      {
        "texto_opcion": "Skip staging and use only seeds",
        "es_correcta": false,
        "explicacion": "Seeds are not live sources."
      }
    ],
    "tags": [
      "1.3",
      "staging pattern"
    ]
  },
  {
    "pregunta": "dbt project review — a BigQuery analytics repo. model depends on a model installed from a dbt package. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Copy package SQL into project manually",
        "es_correcta": false,
        "explicacion": "Forks package updates."
      },
      {
        "texto_opcion": "Hardcode package schema.table",
        "es_correcta": false,
        "explicacion": "Breaks on package upgrades."
      },
      {
        "texto_opcion": "Remove packages.yml",
        "es_correcta": false,
        "explicacion": "Uninstalls package dependencies."
      },
      {
        "texto_opcion": "Use `ref('package_name', 'model_name')` or configure package namespace",
        "es_correcta": true,
        "explicacion": "Package refs resolve models from installed packages in the DAG."
      }
    ],
    "tags": [
      "1.3",
      "package ref"
    ]
  },
  {
    "pregunta": "single model needs table while folder defaults to view. Context: a multi-environment dbt Cloud job.",
    "opciones": [
      {
        "texto_opcion": "Set `{{ config(materialized='table') }}` at top of the model SQL",
        "es_correcta": true,
        "explicacion": "Model-level config overrides folder defaults in dbt_project.yml."
      },
      {
        "texto_opcion": "Change global default to table for all staging",
        "es_correcta": false,
        "explicacion": "Overrides intent for other models."
      },
      {
        "texto_opcion": "Disable the model",
        "es_correcta": false,
        "explicacion": "Does not apply table materialization."
      },
      {
        "texto_opcion": "Edit compiled target/ files",
        "es_correcta": false,
        "explicacion": "Generated artifacts are overwritten."
      }
    ],
    "tags": [
      "1.3",
      "config materialized inline"
    ]
  },
  {
    "pregunta": "exploratory query should not be built in production DAG. Context: a product analytics team using dbt.",
    "opciones": [
      {
        "texto_opcion": "Place SQL in `analyses/` directory",
        "es_correcta": true,
        "explicacion": "analyses compile but are not materialized or run by default."
      },
      {
        "texto_opcion": "Run query only in worksheet and lose version control",
        "es_correcta": false,
        "explicacion": "Not reproducible."
      },
      {
        "texto_opcion": "Add to seeds",
        "es_correcta": false,
        "explicacion": "Seeds are CSV data, not analytical SQL."
      },
      {
        "texto_opcion": "Put ad hoc query in models/ as table",
        "es_correcta": false,
        "explicacion": "Pollutes production DAG."
      }
    ],
    "tags": [
      "1.3",
      "analysis vs model"
    ]
  },
  {
    "pregunta": "For a finance mart with strict SLAs with nightly jobs: run models that are both tag:nightly and path:marts. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Select by UUID",
        "es_correcta": false,
        "explicacion": "dbt selects by name/tag/path, not UUID."
      },
      {
        "texto_opcion": "Use `dbt run --select tag:nightly,path:models/marts`",
        "es_correcta": true,
        "explicacion": "Comma intersection narrows selection to models matching all predicates."
      },
      {
        "texto_opcion": "Run tests with dbt run only",
        "es_correcta": false,
        "explicacion": "dbt run does not execute tests."
      },
      {
        "texto_opcion": "Space-separated select meaning OR incorrectly",
        "es_correcta": false,
        "explicacion": "Graph operator syntax must match docs."
      }
    ],
    "tags": [
      "1.3",
      "node selection intersection"
    ]
  },
  {
    "pregunta": "In a Snowflake warehouse project: declare raw tables for freshness and lineage in docs. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Use exposures for raw tables",
        "es_correcta": false,
        "explicacion": "exposures document downstream usage, not raw sources."
      },
      {
        "texto_opcion": "Define sources in YAML under `sources:` with database/schema/tables",
        "es_correcta": true,
        "explicacion": "sources YAML registers external tables for source() and freshness."
      },
      {
        "texto_opcion": "Only document in README",
        "es_correcta": false,
        "explicacion": "Not parsed by dbt."
      },
      {
        "texto_opcion": "Put sources block inside a .sql model",
        "es_correcta": false,
        "explicacion": "YAML belongs in schema files."
      }
    ],
    "tags": [
      "1.3",
      "sources yaml"
    ]
  },
  {
    "pregunta": "In a multi-environment dbt Cloud job: model SQL must read another dbt model `stg_orders`. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use `SELECT ... FROM {{ ref('stg_orders') }}`",
        "es_correcta": true,
        "explicacion": "ref() resolves dependencies and builds the DAG with environment-aware relations."
      },
      {
        "texto_opcion": "Hardcode `analytics.stg_orders`",
        "es_correcta": false,
        "explicacion": "Bypasses environment routing and lineage."
      },
      {
        "texto_opcion": "Use source() for another dbt model",
        "es_correcta": false,
        "explicacion": "source() is for raw sources, not dbt models."
      },
      {
        "texto_opcion": "Copy stg_orders SQL inline",
        "es_correcta": false,
        "explicacion": "Breaks modularity and lineage."
      }
    ],
    "tags": [
      "1.3",
      "ref function"
    ]
  },
  {
    "pregunta": "dbt project review — a Snowflake warehouse project. model reads raw landing table loaded by ELT outside dbt. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Create empty model with no SQL",
        "es_correcta": false,
        "explicacion": "Does not define the external table as a source."
      },
      {
        "texto_opcion": "Use `{{ source('raw', 'orders') }}` declared in sources YAML",
        "es_correcta": true,
        "explicacion": "source() references declared sources and enables freshness tests."
      },
      {
        "texto_opcion": "Hardcode database.schema.table only",
        "es_correcta": false,
        "explicacion": "Loses source freshness and lineage in docs."
      },
      {
        "texto_opcion": "ref('raw_orders') without a model",
        "es_correcta": false,
        "explicacion": "ref expects a dbt model, not external raw table."
      }
    ],
    "tags": [
      "1.3",
      "source function"
    ]
  },
  {
    "pregunta": "dbt project review — a multi-environment dbt Cloud job. only downstream of `stg_customers` changed and must rebuild. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "--select +stg_customers only upstream",
        "es_correcta": false,
        "explicacion": "Wrong direction; need downstream + suffix."
      },
      {
        "texto_opcion": "dbt run without select every time",
        "es_correcta": false,
        "explicacion": "Rebuilds entire project unnecessarily."
      },
      {
        "texto_opcion": "Manually run warehouse SQL per table",
        "es_correcta": false,
        "explicacion": "Outside dbt orchestration."
      },
      {
        "texto_opcion": "Run `dbt run --select stg_customers+`",
        "es_correcta": true,
        "explicacion": "+ selects downstream dependents in the DAG."
      }
    ],
    "tags": [
      "1.3",
      "dag selection"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a CI pipeline using dbt Core: fix requires rebuilding a mart and all its parents. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "dbt test only",
        "es_correcta": false,
        "explicacion": "Does not rebuild models."
      },
      {
        "texto_opcion": "--select fct_orders+ for parents",
        "es_correcta": false,
        "explicacion": "Suffix + is downstream; prefix + is upstream."
      },
      {
        "texto_opcion": "Drop child models first manually",
        "es_correcta": false,
        "explicacion": "Risky; selector handles order."
      },
      {
        "texto_opcion": "Run `dbt run --select +fct_orders`",
        "es_correcta": true,
        "explicacion": "+ prefix selects upstream ancestors of the model."
      }
    ],
    "tags": [
      "1.3",
      "upstream select"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a medallion lakehouse built with dbt: run staging folder and exclude deprecated models tagged legacy. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use `dbt run --select path:models/staging --exclude tag:legacy`",
        "es_correcta": true,
        "explicacion": "Graph operators combine path, tag, and exclude filters."
      },
      {
        "texto_opcion": "Use only model name with no path/tag",
        "es_correcta": false,
        "explicacion": "Harder to maintain at scale."
      },
      {
        "texto_opcion": "Exclude by renaming files randomly",
        "es_correcta": false,
        "explicacion": "Not declarative."
      },
      {
        "texto_opcion": "Run entire project always",
        "es_correcta": false,
        "explicacion": "Wastes compute."
      }
    ],
    "tags": [
      "1.3",
      "graph operators"
    ]
  },
  {
    "pregunta": "intermediate logic used once should not materialize. Context: a product analytics team using dbt. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use seeds for intermediates",
        "es_correcta": false,
        "explicacion": "Wrong tool for transforms."
      },
      {
        "texto_opcion": "Use ephemeral model or a SQL CTE via ref to ephemeral",
        "es_correcta": true,
        "explicacion": "Ephemeral/CTE avoids extra tables in the warehouse."
      },
      {
        "texto_opcion": "Duplicate SQL in five models",
        "es_correcta": false,
        "explicacion": "Maintenance burden."
      },
      {
        "texto_opcion": "Materialize every step as table",
        "es_correcta": false,
        "explicacion": "Table sprawl and cost."
      }
    ],
    "tags": [
      "1.3",
      "cte vs ephemeral"
    ]
  },
  {
    "pregunta": "dbt project review — a BigQuery analytics repo. dbt compile fails citing a cyclic dependency. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Remove circular ref() chains so the DAG is acyclic",
        "es_correcta": true,
        "explicacion": "dbt requires a directed acyclic graph; cycles break compilation."
      },
      {
        "texto_opcion": "Ignore the error and run --full-refresh",
        "es_correcta": false,
        "explicacion": "Cycle remains; run fails."
      },
      {
        "texto_opcion": "Add cycles for 'performance'",
        "es_correcta": false,
        "explicacion": "Invalid dbt design."
      },
      {
        "texto_opcion": "Replace ref() with hardcoded tables",
        "es_correcta": false,
        "explicacion": "Breaks lineage permanently."
      }
    ],
    "tags": [
      "1.3",
      "cycle detection"
    ]
  },
  {
    "pregunta": "For a multi-environment dbt Cloud job with nightly jobs: raw sources should be lightly cleaned before marts. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "One monolithic 5000-line model",
        "es_correcta": false,
        "explicacion": "Hard to test and maintain."
      },
      {
        "texto_opcion": "Skip staging and use only seeds",
        "es_correcta": false,
        "explicacion": "Seeds are not live sources."
      },
      {
        "texto_opcion": "Build staging models with source() then ref staging from marts",
        "es_correcta": true,
        "explicacion": "Staging isolates source changes and standardizes naming."
      },
      {
        "texto_opcion": "Mart models source() directly with heavy logic",
        "es_correcta": false,
        "explicacion": "Couples marts to raw layout."
      }
    ],
    "tags": [
      "1.3",
      "staging pattern"
    ]
  },
  {
    "pregunta": "dbt project review — a platform team publishing shared packages. model depends on a model installed from a dbt package. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Remove packages.yml",
        "es_correcta": false,
        "explicacion": "Uninstalls package dependencies."
      },
      {
        "texto_opcion": "Use `ref('package_name', 'model_name')` or configure package namespace",
        "es_correcta": true,
        "explicacion": "Package refs resolve models from installed packages in the DAG."
      },
      {
        "texto_opcion": "Copy package SQL into project manually",
        "es_correcta": false,
        "explicacion": "Forks package updates."
      },
      {
        "texto_opcion": "Hardcode package schema.table",
        "es_correcta": false,
        "explicacion": "Breaks on package upgrades."
      }
    ],
    "tags": [
      "1.3",
      "package ref"
    ]
  },
  {
    "pregunta": "For a Databricks SQL warehouse deployment with nightly jobs: single model needs table while folder defaults to view. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Set `{{ config(materialized='table') }}` at top of the model SQL",
        "es_correcta": true,
        "explicacion": "Model-level config overrides folder defaults in dbt_project.yml."
      },
      {
        "texto_opcion": "Disable the model",
        "es_correcta": false,
        "explicacion": "Does not apply table materialization."
      },
      {
        "texto_opcion": "Change global default to table for all staging",
        "es_correcta": false,
        "explicacion": "Overrides intent for other models."
      },
      {
        "texto_opcion": "Edit compiled target/ files",
        "es_correcta": false,
        "explicacion": "Generated artifacts are overwritten."
      }
    ],
    "tags": [
      "1.3",
      "config materialized inline"
    ]
  },
  {
    "pregunta": "For a platform team publishing shared packages with nightly jobs: exploratory query should not be built in production DAG. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Run query only in worksheet and lose version control",
        "es_correcta": false,
        "explicacion": "Not reproducible."
      },
      {
        "texto_opcion": "Place SQL in `analyses/` directory",
        "es_correcta": true,
        "explicacion": "analyses compile but are not materialized or run by default."
      },
      {
        "texto_opcion": "Put ad hoc query in models/ as table",
        "es_correcta": false,
        "explicacion": "Pollutes production DAG."
      },
      {
        "texto_opcion": "Add to seeds",
        "es_correcta": false,
        "explicacion": "Seeds are CSV data, not analytical SQL."
      }
    ],
    "tags": [
      "1.3",
      "analysis vs model"
    ]
  },
  {
    "pregunta": "For a platform team publishing shared packages with nightly jobs: run models that are both tag:nightly and path:marts. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Space-separated select meaning OR incorrectly",
        "es_correcta": false,
        "explicacion": "Graph operator syntax must match docs."
      },
      {
        "texto_opcion": "Run tests with dbt run only",
        "es_correcta": false,
        "explicacion": "dbt run does not execute tests."
      },
      {
        "texto_opcion": "Use `dbt run --select tag:nightly,path:models/marts`",
        "es_correcta": true,
        "explicacion": "Comma intersection narrows selection to models matching all predicates."
      },
      {
        "texto_opcion": "Select by UUID",
        "es_correcta": false,
        "explicacion": "dbt selects by name/tag/path, not UUID."
      }
    ],
    "tags": [
      "1.3",
      "node selection intersection"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a platform team publishing shared packages: declare raw tables for freshness and lineage in docs. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Define sources in YAML under `sources:` with database/schema/tables",
        "es_correcta": true,
        "explicacion": "sources YAML registers external tables for source() and freshness."
      },
      {
        "texto_opcion": "Only document in README",
        "es_correcta": false,
        "explicacion": "Not parsed by dbt."
      },
      {
        "texto_opcion": "Put sources block inside a .sql model",
        "es_correcta": false,
        "explicacion": "YAML belongs in schema files."
      },
      {
        "texto_opcion": "Use exposures for raw tables",
        "es_correcta": false,
        "explicacion": "exposures document downstream usage, not raw sources."
      }
    ],
    "tags": [
      "1.3",
      "sources yaml"
    ]
  },
  {
    "pregunta": "In a Snowflake warehouse project: model SQL must read another dbt model `stg_orders`. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Copy stg_orders SQL inline",
        "es_correcta": false,
        "explicacion": "Breaks modularity and lineage."
      },
      {
        "texto_opcion": "Use source() for another dbt model",
        "es_correcta": false,
        "explicacion": "source() is for raw sources, not dbt models."
      },
      {
        "texto_opcion": "Use `SELECT ... FROM {{ ref('stg_orders') }}`",
        "es_correcta": true,
        "explicacion": "ref() resolves dependencies and builds the DAG with environment-aware relations."
      },
      {
        "texto_opcion": "Hardcode `analytics.stg_orders`",
        "es_correcta": false,
        "explicacion": "Bypasses environment routing and lineage."
      }
    ],
    "tags": [
      "1.3",
      "ref function"
    ]
  },
  {
    "pregunta": "For a multi-environment dbt Cloud job with nightly jobs: model reads raw landing table loaded by ELT outside dbt. Best practice? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Hardcode database.schema.table only",
        "es_correcta": false,
        "explicacion": "Loses source freshness and lineage in docs."
      },
      {
        "texto_opcion": "Use `{{ source('raw', 'orders') }}` declared in sources YAML",
        "es_correcta": true,
        "explicacion": "source() references declared sources and enables freshness tests."
      },
      {
        "texto_opcion": "ref('raw_orders') without a model",
        "es_correcta": false,
        "explicacion": "ref expects a dbt model, not external raw table."
      },
      {
        "texto_opcion": "Create empty model with no SQL",
        "es_correcta": false,
        "explicacion": "Does not define the external table as a source."
      }
    ],
    "tags": [
      "1.3",
      "source function"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: only downstream of `stg_customers` changed and must rebuild. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "--select +stg_customers only upstream",
        "es_correcta": false,
        "explicacion": "Wrong direction; need downstream + suffix."
      },
      {
        "texto_opcion": "Run `dbt run --select stg_customers+`",
        "es_correcta": true,
        "explicacion": "+ selects downstream dependents in the DAG."
      },
      {
        "texto_opcion": "dbt run without select every time",
        "es_correcta": false,
        "explicacion": "Rebuilds entire project unnecessarily."
      },
      {
        "texto_opcion": "Manually run warehouse SQL per table",
        "es_correcta": false,
        "explicacion": "Outside dbt orchestration."
      }
    ],
    "tags": [
      "1.3",
      "dag selection"
    ]
  },
  {
    "pregunta": "fix requires rebuilding a mart and all its parents. Context: a BigQuery analytics repo. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Drop child models first manually",
        "es_correcta": false,
        "explicacion": "Risky; selector handles order."
      },
      {
        "texto_opcion": "dbt test only",
        "es_correcta": false,
        "explicacion": "Does not rebuild models."
      },
      {
        "texto_opcion": "Run `dbt run --select +fct_orders`",
        "es_correcta": true,
        "explicacion": "+ prefix selects upstream ancestors of the model."
      },
      {
        "texto_opcion": "--select fct_orders+ for parents",
        "es_correcta": false,
        "explicacion": "Suffix + is downstream; prefix + is upstream."
      }
    ],
    "tags": [
      "1.3",
      "upstream select"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a platform team publishing shared packages: run staging folder and exclude deprecated models tagged legacy. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Use only model name with no path/tag",
        "es_correcta": false,
        "explicacion": "Harder to maintain at scale."
      },
      {
        "texto_opcion": "Exclude by renaming files randomly",
        "es_correcta": false,
        "explicacion": "Not declarative."
      },
      {
        "texto_opcion": "Use `dbt run --select path:models/staging --exclude tag:legacy`",
        "es_correcta": true,
        "explicacion": "Graph operators combine path, tag, and exclude filters."
      },
      {
        "texto_opcion": "Run entire project always",
        "es_correcta": false,
        "explicacion": "Wastes compute."
      }
    ],
    "tags": [
      "1.3",
      "graph operators"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. intermediate logic used once should not materialize. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Use seeds for intermediates",
        "es_correcta": false,
        "explicacion": "Wrong tool for transforms."
      },
      {
        "texto_opcion": "Duplicate SQL in five models",
        "es_correcta": false,
        "explicacion": "Maintenance burden."
      },
      {
        "texto_opcion": "Use ephemeral model or a SQL CTE via ref to ephemeral",
        "es_correcta": true,
        "explicacion": "Ephemeral/CTE avoids extra tables in the warehouse."
      },
      {
        "texto_opcion": "Materialize every step as table",
        "es_correcta": false,
        "explicacion": "Table sprawl and cost."
      }
    ],
    "tags": [
      "1.3",
      "cte vs ephemeral"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: dbt compile fails citing a cyclic dependency. Best practice? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Add cycles for 'performance'",
        "es_correcta": false,
        "explicacion": "Invalid dbt design."
      },
      {
        "texto_opcion": "Ignore the error and run --full-refresh",
        "es_correcta": false,
        "explicacion": "Cycle remains; run fails."
      },
      {
        "texto_opcion": "Replace ref() with hardcoded tables",
        "es_correcta": false,
        "explicacion": "Breaks lineage permanently."
      },
      {
        "texto_opcion": "Remove circular ref() chains so the DAG is acyclic",
        "es_correcta": true,
        "explicacion": "dbt requires a directed acyclic graph; cycles break compilation."
      }
    ],
    "tags": [
      "1.3",
      "cycle detection"
    ]
  },
  {
    "pregunta": "dbt project review — a platform team publishing shared packages. raw sources should be lightly cleaned before marts. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "One monolithic 5000-line model",
        "es_correcta": false,
        "explicacion": "Hard to test and maintain."
      },
      {
        "texto_opcion": "Build staging models with source() then ref staging from marts",
        "es_correcta": true,
        "explicacion": "Staging isolates source changes and standardizes naming."
      },
      {
        "texto_opcion": "Skip staging and use only seeds",
        "es_correcta": false,
        "explicacion": "Seeds are not live sources."
      },
      {
        "texto_opcion": "Mart models source() directly with heavy logic",
        "es_correcta": false,
        "explicacion": "Couples marts to raw layout."
      }
    ],
    "tags": [
      "1.3",
      "staging pattern"
    ]
  },
  {
    "pregunta": "dbt project review — a platform team publishing shared packages. model depends on a model installed from a dbt package. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Copy package SQL into project manually",
        "es_correcta": false,
        "explicacion": "Forks package updates."
      },
      {
        "texto_opcion": "Remove packages.yml",
        "es_correcta": false,
        "explicacion": "Uninstalls package dependencies."
      },
      {
        "texto_opcion": "Hardcode package schema.table",
        "es_correcta": false,
        "explicacion": "Breaks on package upgrades."
      },
      {
        "texto_opcion": "Use `ref('package_name', 'model_name')` or configure package namespace",
        "es_correcta": true,
        "explicacion": "Package refs resolve models from installed packages in the DAG."
      }
    ],
    "tags": [
      "1.3",
      "package ref"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a CI pipeline using dbt Core: single model needs table while folder defaults to view. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Disable the model",
        "es_correcta": false,
        "explicacion": "Does not apply table materialization."
      },
      {
        "texto_opcion": "Edit compiled target/ files",
        "es_correcta": false,
        "explicacion": "Generated artifacts are overwritten."
      },
      {
        "texto_opcion": "Set `{{ config(materialized='table') }}` at top of the model SQL",
        "es_correcta": true,
        "explicacion": "Model-level config overrides folder defaults in dbt_project.yml."
      },
      {
        "texto_opcion": "Change global default to table for all staging",
        "es_correcta": false,
        "explicacion": "Overrides intent for other models."
      }
    ],
    "tags": [
      "1.3",
      "config materialized inline"
    ]
  },
  {
    "pregunta": "For a Databricks SQL warehouse deployment with nightly jobs: exploratory query should not be built in production DAG. Best practice? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Add to seeds",
        "es_correcta": false,
        "explicacion": "Seeds are CSV data, not analytical SQL."
      },
      {
        "texto_opcion": "Place SQL in `analyses/` directory",
        "es_correcta": true,
        "explicacion": "analyses compile but are not materialized or run by default."
      },
      {
        "texto_opcion": "Run query only in worksheet and lose version control",
        "es_correcta": false,
        "explicacion": "Not reproducible."
      },
      {
        "texto_opcion": "Put ad hoc query in models/ as table",
        "es_correcta": false,
        "explicacion": "Pollutes production DAG."
      }
    ],
    "tags": [
      "1.3",
      "analysis vs model"
    ]
  },
  {
    "pregunta": "For a finance mart with strict SLAs with nightly jobs: run models that are both tag:nightly and path:marts. Best practice? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Run tests with dbt run only",
        "es_correcta": false,
        "explicacion": "dbt run does not execute tests."
      },
      {
        "texto_opcion": "Select by UUID",
        "es_correcta": false,
        "explicacion": "dbt selects by name/tag/path, not UUID."
      },
      {
        "texto_opcion": "Space-separated select meaning OR incorrectly",
        "es_correcta": false,
        "explicacion": "Graph operator syntax must match docs."
      },
      {
        "texto_opcion": "Use `dbt run --select tag:nightly,path:models/marts`",
        "es_correcta": true,
        "explicacion": "Comma intersection narrows selection to models matching all predicates."
      }
    ],
    "tags": [
      "1.3",
      "node selection intersection"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: declare raw tables for freshness and lineage in docs. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Put sources block inside a .sql model",
        "es_correcta": false,
        "explicacion": "YAML belongs in schema files."
      },
      {
        "texto_opcion": "Only document in README",
        "es_correcta": false,
        "explicacion": "Not parsed by dbt."
      },
      {
        "texto_opcion": "Define sources in YAML under `sources:` with database/schema/tables",
        "es_correcta": true,
        "explicacion": "sources YAML registers external tables for source() and freshness."
      },
      {
        "texto_opcion": "Use exposures for raw tables",
        "es_correcta": false,
        "explicacion": "exposures document downstream usage, not raw sources."
      }
    ],
    "tags": [
      "1.3",
      "sources yaml"
    ]
  },
  {
    "pregunta": "model SQL must read another dbt model `stg_orders`. Context: a monorepo with three dbt projects. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Copy stg_orders SQL inline",
        "es_correcta": false,
        "explicacion": "Breaks modularity and lineage."
      },
      {
        "texto_opcion": "Use source() for another dbt model",
        "es_correcta": false,
        "explicacion": "source() is for raw sources, not dbt models."
      },
      {
        "texto_opcion": "Hardcode `analytics.stg_orders`",
        "es_correcta": false,
        "explicacion": "Bypasses environment routing and lineage."
      },
      {
        "texto_opcion": "Use `SELECT ... FROM {{ ref('stg_orders') }}`",
        "es_correcta": true,
        "explicacion": "ref() resolves dependencies and builds the DAG with environment-aware relations."
      }
    ],
    "tags": [
      "1.3",
      "ref function"
    ]
  },
  {
    "pregunta": "model reads raw landing table loaded by ELT outside dbt. Context: a CI pipeline using dbt Core. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Hardcode database.schema.table only",
        "es_correcta": false,
        "explicacion": "Loses source freshness and lineage in docs."
      },
      {
        "texto_opcion": "Use `{{ source('raw', 'orders') }}` declared in sources YAML",
        "es_correcta": true,
        "explicacion": "source() references declared sources and enables freshness tests."
      },
      {
        "texto_opcion": "Create empty model with no SQL",
        "es_correcta": false,
        "explicacion": "Does not define the external table as a source."
      },
      {
        "texto_opcion": "ref('raw_orders') without a model",
        "es_correcta": false,
        "explicacion": "ref expects a dbt model, not external raw table."
      }
    ],
    "tags": [
      "1.3",
      "source function"
    ]
  },
  {
    "pregunta": "For a monorepo with three dbt projects with nightly jobs: only downstream of `stg_customers` changed and must rebuild. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "--select +stg_customers only upstream",
        "es_correcta": false,
        "explicacion": "Wrong direction; need downstream + suffix."
      },
      {
        "texto_opcion": "dbt run without select every time",
        "es_correcta": false,
        "explicacion": "Rebuilds entire project unnecessarily."
      },
      {
        "texto_opcion": "Run `dbt run --select stg_customers+`",
        "es_correcta": true,
        "explicacion": "+ selects downstream dependents in the DAG."
      },
      {
        "texto_opcion": "Manually run warehouse SQL per table",
        "es_correcta": false,
        "explicacion": "Outside dbt orchestration."
      }
    ],
    "tags": [
      "1.3",
      "dag selection"
    ]
  },
  {
    "pregunta": "For a BigQuery analytics repo with nightly jobs: fix requires rebuilding a mart and all its parents. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Drop child models first manually",
        "es_correcta": false,
        "explicacion": "Risky; selector handles order."
      },
      {
        "texto_opcion": "Run `dbt run --select +fct_orders`",
        "es_correcta": true,
        "explicacion": "+ prefix selects upstream ancestors of the model."
      },
      {
        "texto_opcion": "dbt test only",
        "es_correcta": false,
        "explicacion": "Does not rebuild models."
      },
      {
        "texto_opcion": "--select fct_orders+ for parents",
        "es_correcta": false,
        "explicacion": "Suffix + is downstream; prefix + is upstream."
      }
    ],
    "tags": [
      "1.3",
      "upstream select"
    ]
  },
  {
    "pregunta": "run staging folder and exclude deprecated models tagged legacy. Context: a finance mart with strict SLAs. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Use `dbt run --select path:models/staging --exclude tag:legacy`",
        "es_correcta": true,
        "explicacion": "Graph operators combine path, tag, and exclude filters."
      },
      {
        "texto_opcion": "Exclude by renaming files randomly",
        "es_correcta": false,
        "explicacion": "Not declarative."
      },
      {
        "texto_opcion": "Use only model name with no path/tag",
        "es_correcta": false,
        "explicacion": "Harder to maintain at scale."
      },
      {
        "texto_opcion": "Run entire project always",
        "es_correcta": false,
        "explicacion": "Wastes compute."
      }
    ],
    "tags": [
      "1.3",
      "graph operators"
    ]
  },
  {
    "pregunta": "For a CI pipeline using dbt Core with nightly jobs: intermediate logic used once should not materialize. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Use seeds for intermediates",
        "es_correcta": false,
        "explicacion": "Wrong tool for transforms."
      },
      {
        "texto_opcion": "Materialize every step as table",
        "es_correcta": false,
        "explicacion": "Table sprawl and cost."
      },
      {
        "texto_opcion": "Duplicate SQL in five models",
        "es_correcta": false,
        "explicacion": "Maintenance burden."
      },
      {
        "texto_opcion": "Use ephemeral model or a SQL CTE via ref to ephemeral",
        "es_correcta": true,
        "explicacion": "Ephemeral/CTE avoids extra tables in the warehouse."
      }
    ],
    "tags": [
      "1.3",
      "cte vs ephemeral"
    ]
  },
  {
    "pregunta": "In a CI pipeline using dbt Core: dbt compile fails citing a cyclic dependency. Which dbt approach is best? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Add cycles for 'performance'",
        "es_correcta": false,
        "explicacion": "Invalid dbt design."
      },
      {
        "texto_opcion": "Ignore the error and run --full-refresh",
        "es_correcta": false,
        "explicacion": "Cycle remains; run fails."
      },
      {
        "texto_opcion": "Remove circular ref() chains so the DAG is acyclic",
        "es_correcta": true,
        "explicacion": "dbt requires a directed acyclic graph; cycles break compilation."
      },
      {
        "texto_opcion": "Replace ref() with hardcoded tables",
        "es_correcta": false,
        "explicacion": "Breaks lineage permanently."
      }
    ],
    "tags": [
      "1.3",
      "cycle detection"
    ]
  },
  {
    "pregunta": "For a finance mart with strict SLAs with nightly jobs: raw sources should be lightly cleaned before marts. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Skip staging and use only seeds",
        "es_correcta": false,
        "explicacion": "Seeds are not live sources."
      },
      {
        "texto_opcion": "One monolithic 5000-line model",
        "es_correcta": false,
        "explicacion": "Hard to test and maintain."
      },
      {
        "texto_opcion": "Mart models source() directly with heavy logic",
        "es_correcta": false,
        "explicacion": "Couples marts to raw layout."
      },
      {
        "texto_opcion": "Build staging models with source() then ref staging from marts",
        "es_correcta": true,
        "explicacion": "Staging isolates source changes and standardizes naming."
      }
    ],
    "tags": [
      "1.3",
      "staging pattern"
    ]
  },
  {
    "pregunta": "For a product analytics team using dbt with nightly jobs: model depends on a model installed from a dbt package. Best practice? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Use `ref('package_name', 'model_name')` or configure package namespace",
        "es_correcta": true,
        "explicacion": "Package refs resolve models from installed packages in the DAG."
      },
      {
        "texto_opcion": "Remove packages.yml",
        "es_correcta": false,
        "explicacion": "Uninstalls package dependencies."
      },
      {
        "texto_opcion": "Copy package SQL into project manually",
        "es_correcta": false,
        "explicacion": "Forks package updates."
      },
      {
        "texto_opcion": "Hardcode package schema.table",
        "es_correcta": false,
        "explicacion": "Breaks on package upgrades."
      }
    ],
    "tags": [
      "1.3",
      "package ref"
    ]
  }
]
