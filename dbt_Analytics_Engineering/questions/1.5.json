[
  {
    "pregunta": "In a platform team publishing shared packages: transform requires Python dataframe logic on Snowflake. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Embed Python in a SQL macro only, sQL macros cannot run dataframe Python.",
        "es_correcta": false,
        "explicacion": "SQL macros cannot run dataframe Python."
      },
      {
        "texto_opcion": "Create a Python model with `dbt-py` config and dataframe.",
        "es_correcta": true,
        "explicacion": "Python models run Python transformations where adapter supports them."
      },
      {
        "texto_opcion": "Use seeds for Python transforms, seeds are static tables, seeds are static tables.",
        "es_correcta": false,
        "explicacion": "Seeds are static tables."
      },
      {
        "texto_opcion": "Run notebook outside dbt always, bypasses dbt DAG if never integrated.",
        "es_correcta": false,
        "explicacion": "Bypasses dbt DAG if never integrated."
      }
    ],
    "tags": [
      "1.5",
      "python model"
    ]
  },
  {
    "pregunta": "For a finance mart with strict SLAs with nightly jobs: project name must match profile and package namespace expectations. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Rename folder without updating name key, confusing but name key is authoritative for dbt.",
        "es_correcta": false,
        "explicacion": "Confusing but name key is authoritative for dbt."
      },
      {
        "texto_opcion": "Set top-level `name:` key in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "name defines the project identifier used in refs and packages."
      },
      {
        "texto_opcion": "Set project name only in README, dbt reads dbt_project.yml name as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "dbt reads dbt_project.yml name."
      },
      {
        "texto_opcion": "Put name in packages.yml as the primary design for this requirement used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      }
    ],
    "tags": [
      "1.5",
      "dbt_project name"
    ]
  },
  {
    "pregunta": "In a platform team publishing shared packages: CI should use dev target; production job uses prod target. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Configure outputs in profiles.yml and pass `--target prod`.",
        "es_correcta": true,
        "explicacion": "--target selects which profile output dbt connects to."
      },
      {
        "texto_opcion": "Use same schema for dev and prod always, risk of overwriting production.",
        "es_correcta": false,
        "explicacion": "Risk of overwriting production."
      },
      {
        "texto_opcion": "Hardcode prod credentials in model SQL, security anti-pattern, security anti-pattern.",
        "es_correcta": false,
        "explicacion": "Security anti-pattern."
      },
      {
        "texto_opcion": "Store profiles in git with passwords, secrets should use env_var or secret stores.",
        "es_correcta": false,
        "explicacion": "Secrets should use env_var or secret stores."
      }
    ],
    "tags": [
      "1.5",
      "profile target"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: models live under custom directory `transform/`. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Require models only in analyses/, analyses are not materialized models prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "analyses are not materialized models."
      },
      {
        "texto_opcion": "Put models in random repo folders without config, dbt will not compile them as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "dbt will not compile them."
      },
      {
        "texto_opcion": "Change model-paths in profiles.yml, belongs in dbt_project.yml, belongs in dbt_project.yml.",
        "es_correcta": false,
        "explicacion": "Belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Set `model-paths: ['transform']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "model-paths tells dbt where to find models."
      }
    ],
    "tags": [
      "1.5",
      "model paths"
    ]
  },
  {
    "pregunta": "For a Snowflake warehouse project with nightly jobs: seed `country_codes` should land in reference schema. Best practice?",
    "opciones": [
      {
        "texto_opcion": "Use source() for seed file, seeds use ref() after load, sources are external often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "Seeds use ref() after load, sources are external."
      },
      {
        "texto_opcion": "Load seed manually to production, outside dbt reproducibility, outside dbt reproducibility used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Outside dbt reproducibility."
      },
      {
        "texto_opcion": "Configure `seeds: project: country_codes: +schema: reference` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Seed config sets schema, column types, and tags."
      },
      {
        "texto_opcion": "Disable seed in prod without config, use enabled: false on seed config prioritized for simplicity, though.",
        "es_correcta": false,
        "explicacion": "Use enabled: false on seed config."
      }
    ],
    "tags": [
      "1.5",
      "seeds config"
    ]
  },
  {
    "pregunta": "define default `lookback_days: 7` for all runs. Context: a Databricks SQL warehouse deployment.",
    "opciones": [
      {
        "texto_opcion": "Set `vars: { lookback_days: 7 }` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Project-level vars provide defaults overridable via CLI."
      },
      {
        "texto_opcion": "Put vars in macro only without defaults used as the main.",
        "es_correcta": false,
        "explicacion": "Less discoverable."
      },
      {
        "texto_opcion": "Use packages.yml for vars adopted as the standard pattern.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Hardcode 7 in every model prioritized for operational.",
        "es_correcta": false,
        "explicacion": "Duplication."
      }
    ],
    "tags": [
      "1.5",
      "vars in project"
    ]
  },
  {
    "pregunta": "In a monorepo with three dbt projects: project uses features requiring dbt 1.8+. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Pin version only in Docker tag without yml adopted as the configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Both should align."
      },
      {
        "texto_opcion": "Skip version check, team may run unsupported features, team may run unsupported features.",
        "es_correcta": false,
        "explicacion": "Team may run unsupported features."
      },
      {
        "texto_opcion": "Set `require-dbt-version: ['>=1.8.0,<2.0.0']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "require-dbt-version guards against incompatible Core versions."
      },
      {
        "texto_opcion": "Downgrade adapter only silently, adapter/Core mismatch causes errors adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Adapter/Core mismatch causes errors."
      }
    ],
    "tags": [
      "1.5",
      "require dbt version"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. artifact folders should be cleaned safely. What should the team do?",
    "opciones": [
      {
        "texto_opcion": "Never clean in CI, stale artifacts can cause confusion but.",
        "es_correcta": false,
        "explicacion": "Stale artifacts can cause confusion but clean is optional."
      },
      {
        "texto_opcion": "Run `dbt clean` to remove paths listed in clean-targets.",
        "es_correcta": true,
        "explicacion": "dbt clean deletes target/, dbt_packages/, etc. per config."
      },
      {
        "texto_opcion": "Delete git directory, destroys version control, destroys version control.",
        "es_correcta": false,
        "explicacion": "Destroys version control."
      },
      {
        "texto_opcion": "Remove models/ with clean, clean-targets should not include models.",
        "es_correcta": false,
        "explicacion": "clean-targets should not include models."
      }
    ],
    "tags": [
      "1.5",
      "clean targets"
    ]
  },
  {
    "pregunta": "In a medallion lakehouse built with dbt: project requires explicit database quoting on Snowflake. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Quote only in compiled SQL manually, config should drive quoting.",
        "es_correcta": false,
        "explicacion": "Config should drive quoting."
      },
      {
        "texto_opcion": "Disable all quoting on case-sensitive objects, may break relation resolution.",
        "es_correcta": false,
        "explicacion": "May break relation resolution."
      },
      {
        "texto_opcion": "Set quoting in schema test YAML only, project-level setting belongs.",
        "es_correcta": false,
        "explicacion": "Project-level setting belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Configure `quoting: { database: true, schema: true, identifier: true }`",
        "es_correcta": true,
        "explicacion": "quoting controls case-sensitive identifier quoting per adapter needs."
      }
    ],
    "tags": [
      "1.5",
      "quoting database"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a platform team publishing shared packages: enable experimental behavior via project flags. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Set `flags:` section in dbt_project.yml per dbt version docs.",
        "es_correcta": true,
        "explicacion": "flags toggle Core features like microbatch or strict validation."
      },
      {
        "texto_opcion": "Use env flags undocumented, prefer documented flags in yml.",
        "es_correcta": false,
        "explicacion": "Prefer documented flags in yml."
      },
      {
        "texto_opcion": "Edit global dbt install binary adopted as the standard plausible at first glance, yet weaker.",
        "es_correcta": false,
        "explicacion": "Not project-portable."
      },
      {
        "texto_opcion": "Ignore flag deprecation warnings forever configured often seen in legacy.",
        "es_correcta": false,
        "explicacion": "Upgrade path blocked."
      }
    ],
    "tags": [
      "1.5",
      "flags"
    ]
  },
  {
    "pregunta": "Python model should append new partitions only. Context: a multi-environment dbt Cloud job.",
    "opciones": [
      {
        "texto_opcion": "Full dataframe reload hourly on TB data as the default plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Too expensive."
      },
      {
        "texto_opcion": "Skip materialization config on Python model — needs explicit materialization like SQL models configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Needs explicit materialization like SQL models."
      },
      {
        "texto_opcion": "Convert everything to SQL unnecessarily when Python required — use Python when justified as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Use Python when justified."
      },
      {
        "texto_opcion": "Implement incremental logic using dbt Python incremental APIs for adapter.",
        "es_correcta": true,
        "explicacion": "Python models support incremental patterns where adapter implements them."
      }
    ],
    "tags": [
      "1.5",
      "python incremental"
    ]
  },
  {
    "pregunta": "In a product analytics team using dbt: snapshot definitions stored under `snapshots/history/`. Which dbt approach is best?",
    "opciones": [
      {
        "texto_opcion": "Manual history table updates configured end-to-end for this workload plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Not automated SCD."
      },
      {
        "texto_opcion": "Configure snapshot-paths in dbt_project.yml and define snapshots YAML/SQL.",
        "es_correcta": true,
        "explicacion": "snapshot-paths locates SCD snapshot definitions."
      },
      {
        "texto_opcion": "Put snapshots in macros/ used as the main path in this architecture configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Wrong directory."
      },
      {
        "texto_opcion": "Use seeds for SCD2, snapshots are for change tracking over time often seen in legacy designs but.",
        "es_correcta": false,
        "explicacion": "Snapshots are for change tracking over time."
      }
    ],
    "tags": [
      "1.5",
      "snapshots path"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: generic tests live in `tests/generic/`. Select the correct answer.",
    "opciones": [
      {
        "texto_opcion": "Put generic tests in models/ adopted as the standard prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "Wrong location."
      },
      {
        "texto_opcion": "Delete tests/ to speed runs, removes quality gates often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "Removes quality gates."
      },
      {
        "texto_opcion": "Configure test-paths and add generic test SQL in tests/.",
        "es_correcta": true,
        "explicacion": "test-paths includes singular and generic test definitions."
      },
      {
        "texto_opcion": "Only use warehouse constraints without dbt tests — misses dbt test reporting.",
        "es_correcta": false,
        "explicacion": "Misses dbt test reporting."
      }
    ],
    "tags": [
      "1.5",
      "tests path"
    ]
  },
  {
    "pregunta": "dbt project review — a Snowflake warehouse project. transform requires Python dataframe logic on Snowflake. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use seeds for Python transforms, seeds are static tables, seeds are static tables.",
        "es_correcta": false,
        "explicacion": "Seeds are static tables."
      },
      {
        "texto_opcion": "Embed Python in a SQL macro only, sQL macros cannot run dataframe Python.",
        "es_correcta": false,
        "explicacion": "SQL macros cannot run dataframe Python."
      },
      {
        "texto_opcion": "Create a Python model with `dbt-py` config and dataframe.",
        "es_correcta": true,
        "explicacion": "Python models run Python transformations where adapter supports them."
      },
      {
        "texto_opcion": "Run notebook outside dbt always, bypasses dbt DAG if never integrated.",
        "es_correcta": false,
        "explicacion": "Bypasses dbt DAG if never integrated."
      }
    ],
    "tags": [
      "1.5",
      "python model"
    ]
  },
  {
    "pregunta": "For a Snowflake warehouse project with nightly jobs: project name must match profile and package namespace expectations. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Set project name only in README, dbt reads dbt_project.yml name adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "dbt reads dbt_project.yml name."
      },
      {
        "texto_opcion": "Put name in packages.yml adopted as the standard pattern for the team adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Rename folder without updating name key, confusing but name key is authoritative for dbt.",
        "es_correcta": false,
        "explicacion": "Confusing but name key is authoritative for dbt."
      },
      {
        "texto_opcion": "Set top-level `name:` key in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "name defines the project identifier used in refs and packages."
      }
    ],
    "tags": [
      "1.5",
      "dbt_project name"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Databricks SQL warehouse deployment: CI should use dev target; production job uses prod target. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Configure outputs in profiles.yml and pass `--target prod`.",
        "es_correcta": true,
        "explicacion": "--target selects which profile output dbt connects to."
      },
      {
        "texto_opcion": "Store profiles in git with passwords, secrets should use env_var or secret stores.",
        "es_correcta": false,
        "explicacion": "Secrets should use env_var or secret stores."
      },
      {
        "texto_opcion": "Use same schema for dev and prod always, risk of overwriting production.",
        "es_correcta": false,
        "explicacion": "Risk of overwriting production."
      },
      {
        "texto_opcion": "Hardcode prod credentials in model SQL, security anti-pattern, security anti-pattern.",
        "es_correcta": false,
        "explicacion": "Security anti-pattern."
      }
    ],
    "tags": [
      "1.5",
      "profile target"
    ]
  },
  {
    "pregunta": "In a finance mart with strict SLAs: models live under custom directory `transform/`. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Change model-paths in profiles.yml, belongs in dbt_project.yml, belongs in dbt_project.yml.",
        "es_correcta": false,
        "explicacion": "Belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Require models only in analyses/, analyses are not materialized models prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "analyses are not materialized models."
      },
      {
        "texto_opcion": "Put models in random repo folders without config, dbt will not compile them selected to meet the constraints described here.",
        "es_correcta": false,
        "explicacion": "dbt will not compile them."
      },
      {
        "texto_opcion": "Set `model-paths: ['transform']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "model-paths tells dbt where to find models."
      }
    ],
    "tags": [
      "1.5",
      "model paths"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a BigQuery analytics repo: seed `country_codes` should land in reference schema. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Load seed manually to production, outside dbt reproducibility, outside dbt reproducibility plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Outside dbt reproducibility."
      },
      {
        "texto_opcion": "Disable seed in prod without config, use enabled: false on seed config prioritized for simplicity, though.",
        "es_correcta": false,
        "explicacion": "Use enabled: false on seed config."
      },
      {
        "texto_opcion": "Use source() for seed file, seeds use ref() after load, sources are external as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "Seeds use ref() after load, sources are external."
      },
      {
        "texto_opcion": "Configure `seeds: project: country_codes: +schema: reference` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Seed config sets schema, column types, and tags."
      }
    ],
    "tags": [
      "1.5",
      "seeds config"
    ]
  },
  {
    "pregunta": "dbt project review — a medallion lakehouse built with dbt. define default `lookback_days: 7` for all runs. What should the team do? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Use packages.yml for vars configured end-to-end for this workload.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Hardcode 7 in every model prioritized for operational.",
        "es_correcta": false,
        "explicacion": "Duplication."
      },
      {
        "texto_opcion": "Set `vars: { lookback_days: 7 }` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Project-level vars provide defaults overridable via CLI."
      },
      {
        "texto_opcion": "Put vars in macro only without defaults configured prioritized for.",
        "es_correcta": false,
        "explicacion": "Less discoverable."
      }
    ],
    "tags": [
      "1.5",
      "vars in project"
    ]
  },
  {
    "pregunta": "In a platform team publishing shared packages: project uses features requiring dbt 1.8+. Which dbt approach is best? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Downgrade adapter only silently, adapter/Core mismatch causes errors adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Adapter/Core mismatch causes errors."
      },
      {
        "texto_opcion": "Skip version check, team may run unsupported features, team may run unsupported features.",
        "es_correcta": false,
        "explicacion": "Team may run unsupported features."
      },
      {
        "texto_opcion": "Pin version only in Docker tag without yml adopted as the prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "Both should align."
      },
      {
        "texto_opcion": "Set `require-dbt-version: ['>=1.8.0,<2.0.0']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "require-dbt-version guards against incompatible Core versions."
      }
    ],
    "tags": [
      "1.5",
      "require dbt version"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a monorepo with three dbt projects: artifact folders should be cleaned safely. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Delete git directory, destroys version control, destroys version control.",
        "es_correcta": false,
        "explicacion": "Destroys version control."
      },
      {
        "texto_opcion": "Never clean in CI, stale artifacts can cause confusion but.",
        "es_correcta": false,
        "explicacion": "Stale artifacts can cause confusion but clean is optional."
      },
      {
        "texto_opcion": "Run `dbt clean` to remove paths listed in clean-targets.",
        "es_correcta": true,
        "explicacion": "dbt clean deletes target/, dbt_packages/, etc. per config."
      },
      {
        "texto_opcion": "Remove models/ with clean, clean-targets should not include models.",
        "es_correcta": false,
        "explicacion": "clean-targets should not include models."
      }
    ],
    "tags": [
      "1.5",
      "clean targets"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a finance mart with strict SLAs: project requires explicit database quoting on Snowflake. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Set quoting in schema test YAML only, project-level setting belongs.",
        "es_correcta": false,
        "explicacion": "Project-level setting belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Configure `quoting: { database: true, schema: true, identifier: true }`",
        "es_correcta": true,
        "explicacion": "quoting controls case-sensitive identifier quoting per adapter needs."
      },
      {
        "texto_opcion": "Quote only in compiled SQL manually, config should drive quoting.",
        "es_correcta": false,
        "explicacion": "Config should drive quoting."
      },
      {
        "texto_opcion": "Disable all quoting on case-sensitive objects, may break relation resolution.",
        "es_correcta": false,
        "explicacion": "May break relation resolution."
      }
    ],
    "tags": [
      "1.5",
      "quoting database"
    ]
  },
  {
    "pregunta": "For a platform team publishing shared packages with nightly jobs: enable experimental behavior via project flags. Best practice? Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Ignore flag deprecation warnings forever selected to meet.",
        "es_correcta": false,
        "explicacion": "Upgrade path blocked."
      },
      {
        "texto_opcion": "Set `flags:` section in dbt_project.yml per dbt version docs.",
        "es_correcta": true,
        "explicacion": "flags toggle Core features like microbatch or strict validation."
      },
      {
        "texto_opcion": "Use env flags undocumented, prefer documented flags in yml.",
        "es_correcta": false,
        "explicacion": "Prefer documented flags in yml."
      },
      {
        "texto_opcion": "Edit global dbt install binary adopted as the standard.",
        "es_correcta": false,
        "explicacion": "Not project-portable."
      }
    ],
    "tags": [
      "1.5",
      "flags"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a finance mart with strict SLAs: Python model should append new partitions only. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Full dataframe reload hourly on TB data used as the main path applied across environments for.",
        "es_correcta": false,
        "explicacion": "Too expensive."
      },
      {
        "texto_opcion": "Implement incremental logic using dbt Python incremental APIs for adapter.",
        "es_correcta": true,
        "explicacion": "Python models support incremental patterns where adapter implements them."
      },
      {
        "texto_opcion": "Convert everything to SQL unnecessarily when Python required — use Python when justified.",
        "es_correcta": false,
        "explicacion": "Use Python when justified."
      },
      {
        "texto_opcion": "Skip materialization config on Python model — needs explicit materialization like SQL models.",
        "es_correcta": false,
        "explicacion": "Needs explicit materialization like SQL models."
      }
    ],
    "tags": [
      "1.5",
      "python incremental"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a medallion lakehouse built with dbt: snapshot definitions stored under `snapshots/history/`. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Put snapshots in macros/ selected to meet the stated constraints applied across environments for often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "Wrong directory."
      },
      {
        "texto_opcion": "Manual history table updates applied across environments for consistency as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Not automated SCD."
      },
      {
        "texto_opcion": "Use seeds for SCD2, snapshots are for change tracking over time selected to meet the constraints described here.",
        "es_correcta": false,
        "explicacion": "Snapshots are for change tracking over time."
      },
      {
        "texto_opcion": "Configure snapshot-paths in dbt_project.yml and define snapshots YAML/SQL.",
        "es_correcta": true,
        "explicacion": "snapshot-paths locates SCD snapshot definitions."
      }
    ],
    "tags": [
      "1.5",
      "snapshots path"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a product analytics team using dbt: generic tests live in `tests/generic/`. Select the correct answer. Constraint set #2.",
    "opciones": [
      {
        "texto_opcion": "Only use warehouse constraints without dbt tests — misses dbt test reporting.",
        "es_correcta": false,
        "explicacion": "Misses dbt test reporting."
      },
      {
        "texto_opcion": "Put generic tests in models/ adopted as the standard plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Wrong location."
      },
      {
        "texto_opcion": "Configure test-paths and add generic test SQL in tests/.",
        "es_correcta": true,
        "explicacion": "test-paths includes singular and generic test definitions."
      },
      {
        "texto_opcion": "Delete tests/ to speed runs, removes quality gates adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Removes quality gates."
      }
    ],
    "tags": [
      "1.5",
      "tests path"
    ]
  },
  {
    "pregunta": "In a multi-environment dbt Cloud job: transform requires Python dataframe logic on Snowflake. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Run notebook outside dbt always, bypasses dbt DAG if never integrated.",
        "es_correcta": false,
        "explicacion": "Bypasses dbt DAG if never integrated."
      },
      {
        "texto_opcion": "Create a Python model with `dbt-py` config and dataframe.",
        "es_correcta": true,
        "explicacion": "Python models run Python transformations where adapter supports them."
      },
      {
        "texto_opcion": "Use seeds for Python transforms, seeds are static tables, seeds are static tables.",
        "es_correcta": false,
        "explicacion": "Seeds are static tables."
      },
      {
        "texto_opcion": "Embed Python in a SQL macro only, sQL macros cannot run dataframe Python.",
        "es_correcta": false,
        "explicacion": "SQL macros cannot run dataframe Python."
      }
    ],
    "tags": [
      "1.5",
      "python model"
    ]
  },
  {
    "pregunta": "dbt project review — a CI pipeline using dbt Core. project name must match profile and package namespace expectations. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Rename folder without updating name key, confusing but name key is authoritative for dbt.",
        "es_correcta": false,
        "explicacion": "Confusing but name key is authoritative for dbt."
      },
      {
        "texto_opcion": "Set project name only in README, dbt reads dbt_project.yml name often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "dbt reads dbt_project.yml name."
      },
      {
        "texto_opcion": "Set top-level `name:` key in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "name defines the project identifier used in refs and packages."
      },
      {
        "texto_opcion": "Put name in packages.yml as the primary design for this requirement as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      }
    ],
    "tags": [
      "1.5",
      "dbt_project name"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a BigQuery analytics repo: CI should use dev target; production job uses prod target. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Hardcode prod credentials in model SQL, security anti-pattern, security anti-pattern.",
        "es_correcta": false,
        "explicacion": "Security anti-pattern."
      },
      {
        "texto_opcion": "Configure outputs in profiles.yml and pass `--target prod`.",
        "es_correcta": true,
        "explicacion": "--target selects which profile output dbt connects to."
      },
      {
        "texto_opcion": "Store profiles in git with passwords, secrets should use env_var or secret stores.",
        "es_correcta": false,
        "explicacion": "Secrets should use env_var or secret stores."
      },
      {
        "texto_opcion": "Use same schema for dev and prod always, risk of overwriting production.",
        "es_correcta": false,
        "explicacion": "Risk of overwriting production."
      }
    ],
    "tags": [
      "1.5",
      "profile target"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. models live under custom directory `transform/`. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Change model-paths in profiles.yml, belongs in dbt_project.yml, belongs in dbt_project.yml.",
        "es_correcta": false,
        "explicacion": "Belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Require models only in analyses/, analyses are not materialized models prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "analyses are not materialized models."
      },
      {
        "texto_opcion": "Put models in random repo folders without config, dbt will not compile them selected to meet the constraints described here.",
        "es_correcta": false,
        "explicacion": "dbt will not compile them."
      },
      {
        "texto_opcion": "Set `model-paths: ['transform']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "model-paths tells dbt where to find models."
      }
    ],
    "tags": [
      "1.5",
      "model paths"
    ]
  },
  {
    "pregunta": "seed `country_codes` should land in reference schema. Context: a CI pipeline using dbt Core. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Disable seed in prod without config, use enabled: false on seed config configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Use enabled: false on seed config."
      },
      {
        "texto_opcion": "Configure `seeds: project: country_codes: +schema: reference` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Seed config sets schema, column types, and tags."
      },
      {
        "texto_opcion": "Use source() for seed file, seeds use ref() after load, sources are external plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Seeds use ref() after load, sources are external."
      },
      {
        "texto_opcion": "Load seed manually to production, outside dbt reproducibility, outside dbt reproducibility applied across environments for operational consistency.",
        "es_correcta": false,
        "explicacion": "Outside dbt reproducibility."
      }
    ],
    "tags": [
      "1.5",
      "seeds config"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Databricks SQL warehouse deployment: define default `lookback_days: 7` for all runs. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Set `vars: { lookback_days: 7 }` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Project-level vars provide defaults overridable via CLI."
      },
      {
        "texto_opcion": "Use packages.yml for vars configured end-to-end for this workload.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Hardcode 7 in every model used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Duplication."
      },
      {
        "texto_opcion": "Put vars in macro only without defaults configured used as the main.",
        "es_correcta": false,
        "explicacion": "Less discoverable."
      }
    ],
    "tags": [
      "1.5",
      "vars in project"
    ]
  },
  {
    "pregunta": "project uses features requiring dbt 1.8+. Context: a BigQuery analytics repo. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Set `require-dbt-version: ['>=1.8.0,<2.0.0']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "require-dbt-version guards against incompatible Core versions."
      },
      {
        "texto_opcion": "Pin version only in Docker tag without yml prioritized for plausible at first glance, yet weaker than the best option.",
        "es_correcta": false,
        "explicacion": "Both should align."
      },
      {
        "texto_opcion": "Downgrade adapter only silently, adapter/Core mismatch causes errors prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "Adapter/Core mismatch causes errors."
      },
      {
        "texto_opcion": "Skip version check, team may run unsupported features, team may run unsupported features.",
        "es_correcta": false,
        "explicacion": "Team may run unsupported features."
      }
    ],
    "tags": [
      "1.5",
      "require dbt version"
    ]
  },
  {
    "pregunta": "dbt project review — a monorepo with three dbt projects. artifact folders should be cleaned safely. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Never clean in CI, stale artifacts can cause confusion but.",
        "es_correcta": false,
        "explicacion": "Stale artifacts can cause confusion but clean is optional."
      },
      {
        "texto_opcion": "Run `dbt clean` to remove paths listed in clean-targets.",
        "es_correcta": true,
        "explicacion": "dbt clean deletes target/, dbt_packages/, etc. per config."
      },
      {
        "texto_opcion": "Remove models/ with clean, clean-targets should not include models.",
        "es_correcta": false,
        "explicacion": "clean-targets should not include models."
      },
      {
        "texto_opcion": "Delete git directory, destroys version control, destroys version control.",
        "es_correcta": false,
        "explicacion": "Destroys version control."
      }
    ],
    "tags": [
      "1.5",
      "clean targets"
    ]
  },
  {
    "pregunta": "In a monorepo with three dbt projects: project requires explicit database quoting on Snowflake. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Disable all quoting on case-sensitive objects, may break relation resolution.",
        "es_correcta": false,
        "explicacion": "May break relation resolution."
      },
      {
        "texto_opcion": "Set quoting in schema test YAML only, project-level setting belongs.",
        "es_correcta": false,
        "explicacion": "Project-level setting belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Configure `quoting: { database: true, schema: true, identifier: true }`",
        "es_correcta": true,
        "explicacion": "quoting controls case-sensitive identifier quoting per adapter needs."
      },
      {
        "texto_opcion": "Quote only in compiled SQL manually, config should drive quoting.",
        "es_correcta": false,
        "explicacion": "Config should drive quoting."
      }
    ],
    "tags": [
      "1.5",
      "quoting database"
    ]
  },
  {
    "pregunta": "dbt project review — a product analytics team using dbt. enable experimental behavior via project flags. What should the team do? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Ignore flag deprecation warnings forever adopted as the.",
        "es_correcta": false,
        "explicacion": "Upgrade path blocked."
      },
      {
        "texto_opcion": "Set `flags:` section in dbt_project.yml per dbt version docs.",
        "es_correcta": true,
        "explicacion": "flags toggle Core features like microbatch or strict validation."
      },
      {
        "texto_opcion": "Use env flags undocumented, prefer documented flags in yml.",
        "es_correcta": false,
        "explicacion": "Prefer documented flags in yml."
      },
      {
        "texto_opcion": "Edit global dbt install binary applied across environments.",
        "es_correcta": false,
        "explicacion": "Not project-portable."
      }
    ],
    "tags": [
      "1.5",
      "flags"
    ]
  },
  {
    "pregunta": "In a medallion lakehouse built with dbt: Python model should append new partitions only. Which dbt approach is best? Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Skip materialization config on Python model — needs explicit materialization like SQL models.",
        "es_correcta": false,
        "explicacion": "Needs explicit materialization like SQL models."
      },
      {
        "texto_opcion": "Convert everything to SQL unnecessarily when Python required — use Python when justified adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Use Python when justified."
      },
      {
        "texto_opcion": "Full dataframe reload hourly on TB data as the primary design used as the main path in this architecture.",
        "es_correcta": false,
        "explicacion": "Too expensive."
      },
      {
        "texto_opcion": "Implement incremental logic using dbt Python incremental APIs for adapter.",
        "es_correcta": true,
        "explicacion": "Python models support incremental patterns where adapter implements them."
      }
    ],
    "tags": [
      "1.5",
      "python incremental"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Databricks SQL warehouse deployment: snapshot definitions stored under `snapshots/history/`. Select the correct answer. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Manual history table updates adopted as the standard pattern for the team configured end-to-end for the stated workload.",
        "es_correcta": false,
        "explicacion": "Not automated SCD."
      },
      {
        "texto_opcion": "Use seeds for SCD2, snapshots are for change tracking over time adopted as the standard pattern for the team.",
        "es_correcta": false,
        "explicacion": "Snapshots are for change tracking over time."
      },
      {
        "texto_opcion": "Put snapshots in macros/ used as the main path in this architecture often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "Wrong directory."
      },
      {
        "texto_opcion": "Configure snapshot-paths in dbt_project.yml and define snapshots YAML/SQL.",
        "es_correcta": true,
        "explicacion": "snapshot-paths locates SCD snapshot definitions."
      }
    ],
    "tags": [
      "1.5",
      "snapshots path"
    ]
  },
  {
    "pregunta": "generic tests live in `tests/generic/`. Context: a Snowflake warehouse project. Constraint set #3.",
    "opciones": [
      {
        "texto_opcion": "Put generic tests in models/ as the default production choice here.",
        "es_correcta": false,
        "explicacion": "Wrong location."
      },
      {
        "texto_opcion": "Only use warehouse constraints without dbt tests, misses dbt test reporting.",
        "es_correcta": false,
        "explicacion": "Misses dbt test reporting."
      },
      {
        "texto_opcion": "Delete tests/ to speed runs, removes quality gates, removes quality gates.",
        "es_correcta": false,
        "explicacion": "Removes quality gates."
      },
      {
        "texto_opcion": "Configure test-paths and add generic test SQL in tests/.",
        "es_correcta": true,
        "explicacion": "test-paths includes singular and generic test definitions."
      }
    ],
    "tags": [
      "1.5",
      "tests path"
    ]
  },
  {
    "pregunta": "transform requires Python dataframe logic on Snowflake. Context: a finance mart with strict SLAs. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Create a Python model with `dbt-py` config and dataframe.",
        "es_correcta": true,
        "explicacion": "Python models run Python transformations where adapter supports them."
      },
      {
        "texto_opcion": "Run notebook outside dbt always, bypasses dbt DAG if never integrated.",
        "es_correcta": false,
        "explicacion": "Bypasses dbt DAG if never integrated."
      },
      {
        "texto_opcion": "Embed Python in a SQL macro only, sQL macros cannot run dataframe Python.",
        "es_correcta": false,
        "explicacion": "SQL macros cannot run dataframe Python."
      },
      {
        "texto_opcion": "Use seeds for Python transforms, seeds are static tables, seeds are static tables.",
        "es_correcta": false,
        "explicacion": "Seeds are static tables."
      }
    ],
    "tags": [
      "1.5",
      "python model"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Databricks SQL warehouse deployment: project name must match profile and package namespace expectations. Select the correct answer. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Rename folder without updating name key — confusing but name key is authoritative for dbt.",
        "es_correcta": false,
        "explicacion": "Confusing but name key is authoritative for dbt."
      },
      {
        "texto_opcion": "Put name in packages.yml prioritized for operational.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Set project name only in README, dbt reads dbt_project.yml name.",
        "es_correcta": false,
        "explicacion": "dbt reads dbt_project.yml name."
      },
      {
        "texto_opcion": "Set top-level `name:` key in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "name defines the project identifier used in refs and packages."
      }
    ],
    "tags": [
      "1.5",
      "dbt_project name"
    ]
  },
  {
    "pregunta": "dbt project review — a BigQuery analytics repo. CI should use dev target; production job uses prod target. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Configure outputs in profiles.yml and pass `--target prod`.",
        "es_correcta": true,
        "explicacion": "--target selects which profile output dbt connects to."
      },
      {
        "texto_opcion": "Hardcode prod credentials in model SQL, security anti-pattern, security anti-pattern.",
        "es_correcta": false,
        "explicacion": "Security anti-pattern."
      },
      {
        "texto_opcion": "Use same schema for dev and prod always, risk of overwriting production.",
        "es_correcta": false,
        "explicacion": "Risk of overwriting production."
      },
      {
        "texto_opcion": "Store profiles in git with passwords, secrets should use env_var or secret stores.",
        "es_correcta": false,
        "explicacion": "Secrets should use env_var or secret stores."
      }
    ],
    "tags": [
      "1.5",
      "profile target"
    ]
  },
  {
    "pregunta": "models live under custom directory `transform/`. Context: a Snowflake warehouse project. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Put models in random repo folders without config, dbt will not compile them as a primary design for this requirement.",
        "es_correcta": false,
        "explicacion": "dbt will not compile them."
      },
      {
        "texto_opcion": "Require models only in analyses/, analyses are not materialized models often seen in legacy designs but not ideal for this case.",
        "es_correcta": false,
        "explicacion": "analyses are not materialized models."
      },
      {
        "texto_opcion": "Change model-paths in profiles.yml, belongs in dbt_project.yml, belongs in dbt_project.yml.",
        "es_correcta": false,
        "explicacion": "Belongs in dbt_project.yml."
      },
      {
        "texto_opcion": "Set `model-paths: ['transform']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "model-paths tells dbt where to find models."
      }
    ],
    "tags": [
      "1.5",
      "model paths"
    ]
  },
  {
    "pregunta": "dbt project review — a finance mart with strict SLAs. seed `country_codes` should land in reference schema. What should the team do? Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Use source() for seed file, seeds use ref() after load, sources are external applied across environments for operational consistency.",
        "es_correcta": false,
        "explicacion": "Seeds use ref() after load, sources are external."
      },
      {
        "texto_opcion": "Load seed manually to production, outside dbt reproducibility, outside dbt reproducibility.",
        "es_correcta": false,
        "explicacion": "Outside dbt reproducibility."
      },
      {
        "texto_opcion": "Configure `seeds: project: country_codes: +schema: reference` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Seed config sets schema, column types, and tags."
      },
      {
        "texto_opcion": "Disable seed in prod without config, use enabled: false on seed config plausible at first glance, yet.",
        "es_correcta": false,
        "explicacion": "Use enabled: false on seed config."
      }
    ],
    "tags": [
      "1.5",
      "seeds config"
    ]
  },
  {
    "pregunta": "define default `lookback_days: 7` for all runs. Context: a platform team publishing shared packages. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Hardcode 7 in every model configured end-to-end for this workload.",
        "es_correcta": false,
        "explicacion": "Duplication."
      },
      {
        "texto_opcion": "Use packages.yml for vars configured end-to-end for this workload.",
        "es_correcta": false,
        "explicacion": "Wrong file."
      },
      {
        "texto_opcion": "Set `vars: { lookback_days: 7 }` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "Project-level vars provide defaults overridable via CLI."
      },
      {
        "texto_opcion": "Put vars in macro only without defaults configured as the primary.",
        "es_correcta": false,
        "explicacion": "Less discoverable."
      }
    ],
    "tags": [
      "1.5",
      "vars in project"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a CI pipeline using dbt Core: project uses features requiring dbt 1.8+. Select the correct answer. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Skip version check, team may run unsupported features, team may run unsupported features.",
        "es_correcta": false,
        "explicacion": "Team may run unsupported features."
      },
      {
        "texto_opcion": "Set `require-dbt-version: ['>=1.8.0,<2.0.0']` in dbt_project.yml.",
        "es_correcta": true,
        "explicacion": "require-dbt-version guards against incompatible Core versions."
      },
      {
        "texto_opcion": "Downgrade adapter only silently, adapter/Core mismatch causes errors prioritized for simplicity, though it misses a key constraint.",
        "es_correcta": false,
        "explicacion": "Adapter/Core mismatch causes errors."
      },
      {
        "texto_opcion": "Pin version only in Docker tag without yml used as the prioritized for as the default production choice in this scenario.",
        "es_correcta": false,
        "explicacion": "Both should align."
      }
    ],
    "tags": [
      "1.5",
      "require dbt version"
    ]
  },
  {
    "pregunta": "During `dbt build` troubleshooting in a Snowflake warehouse project: artifact folders should be cleaned safely. Select the correct answer. Constraint set #4.",
    "opciones": [
      {
        "texto_opcion": "Run `dbt clean` to remove paths listed in clean-targets.",
        "es_correcta": true,
        "explicacion": "dbt clean deletes target/, dbt_packages/, etc. per config."
      },
      {
        "texto_opcion": "Delete git directory, destroys version control, destroys version control.",
        "es_correcta": false,
        "explicacion": "Destroys version control."
      },
      {
        "texto_opcion": "Never clean in CI, stale artifacts can cause confusion but.",
        "es_correcta": false,
        "explicacion": "Stale artifacts can cause confusion but clean is optional."
      },
      {
        "texto_opcion": "Remove models/ with clean, clean-targets should not include models.",
        "es_correcta": false,
        "explicacion": "clean-targets should not include models."
      }
    ],
    "tags": [
      "1.5",
      "clean targets"
    ]
  }
]
